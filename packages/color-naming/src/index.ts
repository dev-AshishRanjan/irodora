/**
 * Nearest-match colour naming.
 *
 * Output is always "closest digital reference", never an assertion of identity
 * (ADR-0031). A rendered hex is a modern approximation of a colour historically
 * produced by a dye on a fibre under daylight.
 */

/** Implemented in F-013. */
export const NAMING_VERSION = '0.0.0' as const;
