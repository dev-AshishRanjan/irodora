/** Rules, weights, scoring and explanation objects. Implemented in F-028 onward. */
export type ExplanationDirection = 'supports' | 'opposes' | 'neutral';
export const RECOMMENDATION_VERSION = '0.0.0' as const;
