{
  "name": "@irodora/color-calibration",
  "version": "0.0.0",
  "private": false,
  "description": "Correct a camera against a reference card whose values somebody else published.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "test:golden": "vitest run --config vitest.golden.config.ts",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@irodora/color-difference": "workspace:*",
    "@irodora/color-spaces": "workspace:*"
  },
  "devDependencies": {
    "@irodora/testing": "workspace:*",
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10"
  }
}
