{
  "name": "@irodora/cvd-engine",
  "version": "0.0.0",
  "private": false,
  "description": "Colour-vision-deficiency simulation and separation scoring.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo",
    "test:golden": "vitest run --config vitest.golden.config.ts",
    "test:cvd": "vitest run --config vitest.cvd.config.ts"
  },
  "dependencies": {
    "@irodora/color-spaces": "workspace:*",
    "@irodora/color-difference": "workspace:*"
  },
  "devDependencies": {
    "@irodora/testing": "workspace:*",
    "@types/culori": "^4.0.1",
    "culori": "^4.0.2",
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10"
  }
}
