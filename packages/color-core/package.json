{
  "name": "@irodora/color-core",
  "version": "0.0.0",
  "private": false,
  "description": "The Colour value type, provenance, sampling and reproducibility envelopes.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@irodora/color-spaces": "workspace:*",
    "@irodora/color-difference": "workspace:*",
    "@irodora/cvd-engine": "workspace:*",
    "@irodora/color-harmony": "workspace:*",
    "@irodora/color-naming": "workspace:*"
  }
}
