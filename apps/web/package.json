{
  "name": "@irodora/web",
  "version": "0.0.0",
  "private": true,
  "description": "Next.js. Atlas, Lens, Compare, Palette Studio. From F-017.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
