{
  "name": "@irodora/admin",
  "version": "0.0.0",
  "private": true,
  "description": "Internal content management. From F-061.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
