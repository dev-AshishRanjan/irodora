{
  "name": "@irodora/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Background jobs — the only process that decodes images.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
