{
  "$schemaRef": "schemas/feature_list.schema.json",
  "version": 1,
  "project": "Irodora",
  "updated": "2026-08-20",
  "policy": {
    "wip_limit": 1,
    "ordering": "Lowest id first within the current release, with every blockedBy done. Release order is deliberate: R1 proves the colour engine in public before anything is built on top of it."
  },
  "statuses": [
    "backlog",
    "todo",
    "in_progress",
    "blocked",
    "in_review",
    "done"
  ],
  "releases": [
    "R0",
    "R1",
    "R2",
    "R3",
    "R4",
    "R5"
  ],
  "features": [
    {
      "id": "F-001",
      "title": "Monorepo toolchain scaffold",
      "requirements": [
        "NFR-24"
      ],
      "service": "root",
      "release": "R0",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-001-monorepo-toolchain-scaffold.md",
      "acceptance": [
        "pnpm workspace with apps/*, packages/*, tests/* resolves and installs",
        "Turborepo pipeline runs typecheck, lint, format:check, test, build across the workspace",
        "TypeScript project references configured; tsconfig.json is the lint/editor project and tsconfig.build.json is the emit project",
        "Package boundary lint fails on a deep import into another package's internals",
        "The colour-engine lint zone fails on node:*, window, document or process",
        "Gates 1-4 and 6 activate in gates.json and run in CI"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "build"
      ],
      "notes": "Node 24 LTS is pinned in .nvmrc. The workstation currently runs 22.16.0 and must be upgraded before this feature starts."
    },
    {
      "id": "F-002",
      "title": "Shared contracts package",
      "requirements": [
        "NFR-24"
      ],
      "service": "packages",
      "package": "@irodora/contracts",
      "release": "R0",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-002-shared-contracts-package.md",
      "blockedBy": [
        "F-001"
      ],
      "acceptance": [
        "Zod schemas are the single source of runtime validation and of the TypeScript types derived from them",
        "A schema change propagates to inferred types with no hand-written duplicate",
        "The error-code enum is closed and versioned",
        "No hand-written type duplicates a schema"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "build"
      ]
    },
    {
      "id": "F-003",
      "title": "OKLCH-native design token package",
      "requirements": [
        "NFR-8",
        "NFR-9"
      ],
      "service": "packages",
      "package": "@irodora/design-tokens",
      "release": "R0",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-003-oklch-native-design-token-package.md",
      "blockedBy": [
        "F-001",
        "F-007",
        "F-008"
      ],
      "acceptance": [
        "design-system.manifest.json compiles to CSS custom properties, TypeScript, React Native styles and a Tailwind v4 theme",
        "The contrast gate reads the manifest and checks every pairsWith combination in both themes",
        "Every status token pairs with an icon token; a status expressible only as colour cannot be constructed",
        "cvdPairs are asserted distinguishable at severity 1.0",
        "Gate 9 (contrast) activates, report-only while the manifest status is placeholder"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "contrast"
      ],
      "notes": "DONE. DELIVERED: the manifest compiles to four targets (CSS custom properties with an @supports OKLCh upgrade, TypeScript constants, React Native styles, a Tailwind v4 theme), every output committed and BYTE-COMPARED by its tests; gate 9 (contrast) ACTIVE and BLOCKING after being watched fail on five real defects, with an eight-case mutation proof in CI (scripts/verify-contrast-proof.mjs) asserting the baseline green in each — seven that must go red, and one that must stay green (a placeholder manifest status with a real failure present, proving the report-only switch); the cvdPairs assertion in gate 10; the status/icon/text triple as a TYPE, so a colour-only status fails typecheck rather than review. THREE DEFECTS FOUND IN THE APPROVED MANIFEST: (1) 37 of 38 opaque tokens had an srgb hex that did not match their own oklch, up to 6.09 dE00 — settled by ADR-0043, srgb is now engine-derived and a hand-edited hex fails the gate; (2) a DUPLICATE JSON KEY meant the top-level \"status\": \"approved\" never survived parsing, so gate.contrast.blockingWhenStatus compared against an object and the gate could never block — the pairing block is renamed statusPairing; (3) five WCAG AA failures and five CVD separation failures in the approved values, corrected in ADR-0044, whose real content is the decision to classify status.* as usage:\"text\" (4.5:1) rather than nonText (3:1). NOT DELIVERED: the half of gate 9 that scans RENDERED surfaces for colour-only status indicators — no component exists until F-017, and the gate prints that on every run. THE COLOUR-SCIENCE REVIEW RETURNED CHANGES REQUIRED AND FOUND FOUR REAL DEFECTS IN THE GATE, ALL FIXED: (a) the comment justifying a single severity check was FALSE — Machado is not monotone, and light status.warn/status.bad under tritan scores 61.2 at severity 0.90 against 65.8 at 1.0, so all eleven tabulated severities are now checked; (b) the separation claim was model-dependent — under Vienot total dichromacy, which color-engine.md assigns to exactly this case, light status.ok/status.warn under protan scored 59.6, BELOW the declared 60, so both models are now checked and the worse taken, and the values moved again to clear it; (c) the alpha-compositing comment was factually INVERTED — for a light overlay on a dark ground the linear reading is 2.2x more PERMISSIVE, not stricter, and the encoded model is what browsers and React Native actually draw, so both are now computed and the worse taken; (d) gate coverage was driven entirely by pairsWith, so nine tokens per theme were checked by nothing while looking exactly like tokens that passed — a token covered by nothing now fails unless it declares an uncheckedReason. ALSO CORRECTED: a wrong unit in ADR-0044 (11/255 is the encoded byte, not the linear value), the chromaCeiling field name (surfaceAndText, when the rule had become universal, was an invitation to re-derive the exemption-by-classification bug from the field name), a missing Lab-illuminant statement on ADR-0043 dE00 column, and a factually wrong comment in packages/cvd-engine/src/machado.ts claiming every reference implementation applies the matrices in encoded sRGB — R colorspace and DaltonLens apply them in linear. The palette was recomputed under BOTH conventions and clears the minimum either way (61.9 linear, 64.0 encoded), which is now recorded there. INDEPENDENTLY VERIFIED on the third evaluator attempt (the first two died on session limits). The evaluation reproduced every gate green and returned FAIL on the RECORD, which no gate reads: ADR-0044 value table showed the intermediate correction rather than what shipped, and DESIGN-SYSTEM.md claimed a TypeScript brand that no code applied — TextToken/LargeTextToken were phantom brands nothing produced. Both fixed; the types are now literal unions DERIVED from the manifest usage field. It also found that NOTHING isolated checkContrast: the one contrast mutation also broke the derived-hex check, so a checkContrast returning passes:true unconditionally left gate 9 exiting 0 and all eight mutations green — verified by neutering it. Now covered by test/check.test.ts and a ninth proof case. Also fixed: turbo.json gave test:contrast no dependency on its own build; the severity-sweep decoy matched rows scoring 100 at every severity; and six stale measurements including the record of a re-measurement that was not itself re-measured. NOT REVIEWED BY A HUMAN: the corrected palette was reviewed by the designer and colour-scientist subagents, not by the person who approved it; the manifest carries a valuesChangedSinceApproval field saying so, because status:approved is the one field a gate reads. All colour maths is delegated to @irodora/color-difference and @irodora/cvd-engine, so nothing here re-implements a metric. FOUND AND RECORDED RATHER THAN FIXED: F-067 (the two themes assert opposite salience hierarchies — pre-existing), F-068 (swatch edge against an arbitrary sample), F-069 (status colour adjacent to a sample). border.strong composited over a light surface reaches 1.17:1; it is in no pairsWith so nothing checks it, and that is recorded rather than silently widened.",
      "effects": [
        "E-007"
      ]
    },
    {
      "id": "F-004",
      "title": "CI/CD pipeline and gate wiring",
      "requirements": [
        "NFR-14",
        "NFR-19"
      ],
      "service": "root",
      "release": "R0",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-004-ci-cd-pipeline-and-gate-wiring.md",
      "notes": "Acceptance criterion 3 (branch protection) is SPECIFIED, not applied: there is no git remote. See docs/operations/branch-protection.md and the observation in memory/observations.md. Everything else is delivered and proven; gate 15 activated after being executed and watched fire.",
      "blockedBy": [
        "F-001"
      ],
      "acceptance": [
        "GitHub Actions runs every active gate in gates.json order, stopping at the first failure",
        "The gates.json to ci.yml mirror check passes, and a deliberately removed step makes it fail",
        "Branch protection requires all checks, one review, and linear history",
        "gitleaks runs on every push; dependency audit blocks on Critical and High",
        "Changesets configured for the publishable packages"
      ],
      "verification": [
        "state",
        "security"
      ],
      "attested": [
        {
          "criterion": "Branch protection requires all checks, one review, and linear history",
          "verifiedBy": "Applying docs/operations/branch-protection.md to the repository once a remote exists",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-006",
      "title": "Colour spaces and conversion",
      "requirements": [
        "FR-1",
        "NFR-1",
        "NFR-3",
        "NFR-19"
      ],
      "service": "packages",
      "package": "@irodora/color-spaces",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-006-colour-spaces-and-conversion.md",
      "blockedBy": [
        "F-001"
      ],
      "notes": "The colour engine starts here. DELIVERED: eight spaces converting in both directions through XYZ D65, CAT16 and Bradford adaptation, five cited golden datasets (64 entries), 56 ordered round-trip pairs within ΔE00 0.01 over 10,000 stratified samples, oracle cross-validation against culori and colorjs.io — BITWISE identical to colorjs.io on XYZ, Lab-D65, P3 and linear sRGB — and gate 5 activated after each dataset was watched reject a real mutation. Zero runtime dependencies is now a CHECK (scripts/verify-engine-purity.mjs), not a claim: lint blocked node:* but nothing blocked `import chroma from 'chroma-js'`. ADR-0040 records the OKLab constants: CSS Color 4's recalculation rather than Ottosson's original ten decimals, which makes D65 white exactly neutral and this package bitwise identical to colorjs.io. ADR-0039 is its superseded predecessor, kept because its reasoning error — reading two oracles agreeing with each other as evidence about THEM — is the useful part. Criterion 5's Node leg and its whole mechanism are gated; the browser and React Native EXECUTIONS are attested (ADR-0038) — a browser ran the vectors once during this feature and matched the digest, but Chromium and Node are both V8, so that proves platform-API freedom rather than engine independence.",
      "acceptance": [
        "sRGB, Display-P3, linear RGB, CIE XYZ (D65), CIELAB, CIELCh, OKLab and OKLCh conversions implemented",
        "CAT16 chromatic adaptation, with Bradford available",
        "Golden datasets from published sources, each citing its source, including near-black values that fail against a pure-power transfer function",
        "Round-trip through every pair within DeltaE00 0.01 across 10,000 sampled colours",
        "Cross-platform identity: Node, browser and React Native produce bitwise-identical output",
        "Zero runtime dependencies; no node:*, DOM or process",
        "Gate 5 (color-golden) activates"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "color-golden",
        "build"
      ],
      "attested": [
        {
          "criterion": "Cross-platform identity: Node, browser and React Native produce bitwise-identical output",
          "verifiedBy": "Executing packages/color-spaces/test/identity/vectors.ts and asserting the committed digest b2cbf7703a642ad7 in a browser under Playwright (F-017) and on a physical device under React Native (F-039/F-040). The mechanism, the fixture and the Node execution are gated by gate 5 today; a browser run during F-006 matched the digest, but Node and Chromium are both V8, so engine independence — specifically whether Hermes' Math.pow and Math.cbrt agree bit for bit — remains unverified.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "effects": [
        "E-001",
        "E-002"
      ]
    },
    {
      "id": "F-007",
      "title": "Colour difference and contrast",
      "requirements": [
        "FR-2",
        "FR-3"
      ],
      "service": "packages",
      "package": "@irodora/color-difference",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-007-colour-difference-and-contrast.md",
      "notes": "DeltaE00 is now the ranking authority in code (E-003). DELIVERED: DeltaE76, DeltaE94 (both weightings), DeltaE00 and DeltaEok; WCAG 2.x contrast; APCA 0.0.98G-4g. All 34 Sharma-Wu-Dalal pairs to four decimal places, each ALSO computed independently by culori so a transcription error in the reference data is caught separately from an implementation error. APCA asserted BITWISE against colorjs.io. Every constant in the package pinned digit-for-digit at tolerance 0 — the check F-006 did not have. THREE luminance definitions now coexist deliberately (engine, WCAG, APCA), each mandated by its own standard and none substitutable: swapping the engine s exact Y row for WCAG s rounded coefficients flips rgb(27,129,156) on white from failing AA to passing, and a sweep finds 111 such flips. Recorded in ADR-0041.",
      "blockedBy": [
        "F-006"
      ],
      "acceptance": [
        "DeltaE76, DeltaE94, DeltaE00 (CIEDE2000) and DeltaEok implemented",
        "All 34 Sharma-Wu-Dalal test pairs reproduced to 4 decimal places",
        "WCAG 2.x contrast ratio matches the specification's worked examples exactly",
        "APCA Lc computed and reported alongside",
        "Property tests assert DeltaE symmetry and identity"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "color-golden",
        "build"
      ],
      "effects": [
        "E-003"
      ]
    },
    {
      "id": "F-008",
      "title": "CVD engine and separation scoring",
      "requirements": [
        "FR-4",
        "FR-5",
        "NFR-10"
      ],
      "service": "packages",
      "package": "@irodora/cvd-engine",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "plan": "plans/F-008-cvd-engine-and-separation-scoring.md",
      "blockedBy": [
        "F-007"
      ],
      "acceptance": [
        "Brettel-Vienot dichromacy and Machado anomalous trichromacy at severity 0 to 1",
        "Dichromat simulation of a published confusion-line pair yields DeltaE00 below 2",
        "One separation score in [0,100], combining post-simulation DeltaE00 with lightness difference, used identically by the UI and the recommendation engine",
        "Gate 10 (cvd) activates"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "color-golden",
        "cvd",
        "build"
      ],
      "effects": [
        "E-005"
      ],
      "notes": "IN PROGRESS. DELIVERED: Machado anomalous trichromacy for all three deficiencies across CONTINUOUS severity (culori snaps to 0.1 steps because its interpolation weight is Math.round(t % 0.1), always 0 — we interpolate, and the oracle is consulted only at the 11 tabulated points); Vienot dichromacy for protan and deutan; one separation score in [0,100] (E-005); gate 10 ACTIVE after being watched fail. NOT DELIVERED — acceptance criterion 1 is only partly met: TRITAN DICHROMACY. Vienot's single-plane simplification is not accurate for tritanopia, whose two half-planes diverge, so simulateDichromacy THROWS for it rather than returning a plausible wrong answer that would feed the separation score. The full two-half-plane Brettel 1997 construction is the remaining work, and there is no oracle for it in either library — the check will have to be the confusion-line property, as it is for the other two. THE BLOCKER IS IDENTIFIED AND PINNED: XYZ_TO_LMS_HPE is the WRONG fundamental set. A copunctal point is the chromaticity of the missing cone fundamental, so it is derivable from the LMS-to-XYZ columns; HPE gives tritan (0.1680, 0.0000) against the published (0.171, -0.003) — agreement to 0.003 — but protan (0.8374, 0.1626) against (0.747, 0.253) and deutan (2.3019, -1.3019) against (1.400, -0.400). The published points are Smith-Pokorny, which is also what Vienot derived from, so the remaining construction needs Smith-Pokorny fundamentals plus the four anchor stimuli (475/575 nm for protan and deutan, 485/660 nm for tritan). The fundamental set is now SETTLED: Smith-Pokorny reproduces all three published copunctal points (protan 0.0007, deutan 0.0002, tritan 0.0048). The ANCHOR STIMULI are the blocker, and an attempt was refuted rather than shipped: recalled anchors disagree with Vienot — which is the published single-plane reduction of Brettel, so a real oracle — by 32.5 dE00 (protan) and 57.4 (deutan), and the diagnostic explains why: the two half-plane normals sit 88.4 degrees apart where Vienot validity requires ~0. They cannot be derived here — computing LMS at a wavelength needs CIE colour-matching functions and there is NO spectral data in the dependency tree and no Brettel implementation to check against. FINISHING THIS NEEDS A DECISION, NOT CODE: vendor CIE colour-matching functions (a content and licensing question per content/AGENTS.md), obtain a trustworthy transcription of Brettel published anchor table, or attest criterion 1 under ADR-0038. Criterion 2 is met for protan and deutan using the kernel of the published Vienot matrix; note that pairs built from the classic published COPUNCTAL POINTS only collapse to ~5.2 dE00, because those points and Vienot's matrices come from different cone fundamentals. Recorded as a golden entry.",
      "attested": [
        {
          "criterion": "Brettel-Vienot dichromacy and Machado anomalous trichromacy at severity 0 to 1",
          "verifiedBy": "TRITAN DICHROMACY ONLY. Everything else in this criterion is gated: Machado anomalous trichromacy for all three deficiencies across continuous severity, and Vienot dichromacy for protan and deutan, are covered by gate 5 and gate 10 (ADR-0038 section 4 — where a part is checkable, that part stays gated). Tritan dichromacy needs the two-half-plane Brettel 1997 construction, which needs the LMS anchor stimuli at 485 and 660 nm. Those cannot be derived or verified here: computing LMS at a wavelength requires CIE colour-matching functions, there is no spectral data in the repository or its dependency tree, and neither culori nor colorjs.io implements Brettel. An attempt using recalled anchors was REFUTED and not shipped — it disagreed with Vienot (the published single-plane reduction of Brettel, and therefore a real oracle for protan and deutan) by 32.5 dE00 protan and 57.4 deutan, and the diagnostic showed the half-plane normals 88.4 degrees apart where Vienot validity requires ~0. DISCHARGED BY: implementing the construction against either vendored CIE colour-matching functions or a trustworthy transcription of Brettel published anchor table, and accepting only if BOTH checks pass — agreement with Vienot on protan and deutan, and near-coplanar half-plane normals. NOTE ON THE BAR: ADR-0038 asks for \"verifying it requires a system we do not control\". This is missing published source data rather than a missing system, which is a weaker case than a VPS or a physical device — recorded plainly rather than glossed. What makes it defensible is that tritan CVD is NOT unsupported: Machado tritanomaly is delivered across full severity and the separation score covers all three deficiencies through the Machado path, so only tritan DICHROMACY specifically is owed.",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-009",
      "title": "Gamut mapping",
      "requirements": [
        "FR-8"
      ],
      "service": "packages",
      "package": "@irodora/color-spaces",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-006"
      ],
      "acceptance": [
        "Out-of-gamut colours map by OKLCh chroma reduction, preserving lightness and hue within stated bounds",
        "Mapping an in-gamut colour is idempotent, asserted by property test",
        "Golden set of known out-of-gamut colours and their mapped results"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "color-golden",
        "build"
      ],
      "plan": "plans/F-009-gamut-mapping.md",
      "effects": [
        "E-012"
      ],
      "notes": "DONE. DELIVERED: gamutMap and gamutMapDetail in @irodora/color-spaces — OKLCh chroma bisection, 32 halvings, holding L and H, for sRGB and Display-P3. Zero new colour maths: every number is an existing engine conversion, and the feature is a search over a predicate. THE DECISION IS ADR-0045 AND IT IS A DELIBERATE DEVIATION FROM CSS COLOR 4. The standard adds a MINDE early stop that returns the CLIPPED colour once it is within a JND in dEok, and dEok tolerates hue movement — measured against colorjs.io toGamut({method:css}) over 30 out-of-gamut colours, CSS drifts hue up to 11.97 degrees where we drift 2.6e-5, and the two results differ by up to 5.21 dE00. Our implementation was cross-checked against culori clampChroma, an independent implementation of the SAME algorithm: agreement 0.0063 dE00. The MINDE step was also structurally unavailable — dEok lives in @irodora/color-difference, which depends on this package, so implementing it would have meant a second implementation of a shipped function. Both reasons are recorded; the measurement is the one that decided it. THE STATED BOUND, which acceptance criterion 1 asks for: L and H are preserved EXACTLY in OKLCh (7e-12, the round-trip noise). After rendering, a final clamp of at most GAMUT_EPSILON=1e-7 per channel applies, and near the black point that tiny absolute movement is a large relative one — hue drift reaches 23 degrees below result L,C=0.01 where OKLCh hue is not a meaningful angle, and is 6.9e-5 degrees above L,C=0.05. Tabulated in the test rather than asserted at one convenient cutoff. THREE CASES ARE NOT CHROMA REDUCTION and each is an explicit branch: already in gamut (the early return is what makes idempotence exact), achromatic, and out of gamut in LIGHTNESS where no chroma fits and lightnessOutOfRange says so rather than a loop returning whatever it last held. THE DECOY is per-channel clipping — the algorithm almost everything else uses — implemented in the test and measured at 33.6 degrees of hue shift, with our own drift asserted beside it as the baseline. FOUND AND RECORDED, NOT FIXED: F-071, two pre-existing flaky property tests. oklab.test.ts round-trips 5000 unseeded fast-check samples against a 1e-12 bound and failed once during this feature at 1.2477e-12 before passing six consecutive full-suite runs; the F-003 evaluation found a second in packages/testing. A blocking gate that can go red for a reason unrelated to the change teaches people to re-run until green."
    },
    {
      "id": "F-010",
      "title": "The Color value type and reproducibility envelope",
      "requirements": [
        "FR-9",
        "FR-10"
      ],
      "service": "packages",
      "package": "@irodora/color-core",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-006"
      ],
      "acceptance": [
        "Color carries required Provenance; an unclassified colour is not constructible",
        "Provenance records source, confidence, conditions and originSpace",
        "Color.unsafeFromHex is the only untracked construction path, and every call site is reviewed",
        "Every derived result carries {engine, corpus, rules, profile}",
        "A regression test pins historical envelopes and replays them byte-identically"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "build"
      ],
      "effects": [
        "E-002"
      ],
      "plan": "plans/F-010-color-value-type-and-reproducibility-envelope.md",
      "notes": "DONE. DELIVERED: the Color value type (canonical XYZ + required Provenance), fromXyz / fromSpace / unsafeFromHex / withProvenance, CaptureConditions, and the ReproducibilityEnvelope with a canonical serialisation and a historical replay fixture. No colour maths: this package is a facade and every conversion is @irodora/color-spaces. THE DESIGN DECISION: Provenance is a DISCRIMINATED UNION on source, not an interface with an optional conditions field. ADR-0005 says conditions is required when the source is estimated or calibrated, and an optional field only asks nicely — an estimate that lost its capture conditions would still compile, which is exactly the object nobody should be able to build. CaptureConditions was NOT invented for this: its fields are specified in docs/architecture/color-engine.md and match FR-17 (illuminant) and FR-18 (quality) exactly, so the union requires real data rather than an empty object. THE UNION BROKE THE WIRE PIN, AND THAT IS THE MECHANISM WORKING. ADR-0036 pins the engine type to the Zod schema at compile time; adding conditions turned it red immediately, which forced the contract to move in the same commit. But the union then WEAKENED the pin in a way that passed silently: keyof on a union returns only the keys COMMON to every member, so an optional field added to one member type-checked cleanly. Verified by doing it. The pin is now asserted PER MEMBER, and scripts/verify-contract-pin.mjs proves it — four probes, each adding an optional field to a different part of the schema, each asserted to turn typecheck red with the baseline asserted green first. EVERY CALL SITE IS REVIEWED, MADE COUNTABLE: scripts/verify-unsafe-call-sites.mjs enumerates unsafeFromHex call sites and runs inside pnpm lint; a new one fails the build until someone adds it to the reviewed list, which is the moment the review happens. It is a script rather than a test because the colour-engine ESLint zone forbids node: imports even in tests (NFR-3), and the alternative was weakening the strictest guard in the repository. THE REPLAY FIXTURE PINS HISTORICAL VERSIONS, not current ones, and asserts it: a fixture written from todays CORE_VERSION compares the code to itself and goes green for free. That assertion caught one of my own entries using 0.1.0."
    },
    {
      "id": "F-011",
      "title": "Corpus schema, provenance and the content gate",
      "requirements": [
        "FR-21",
        "FR-23",
        "FR-24",
        "FR-25",
        "NFR-20",
        "FR-68"
      ],
      "service": "content",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-010"
      ],
      "acceptance": [
        "Entry schema per docs/content/color-corpus-spec.md, with all provenance fields required",
        "Five classifications enforced and displayed; our own curation cannot be marked historical",
        "Derived values (lab, oklch, hex) computed from xyz by the engine at build time, never typed",
        "Editorial workflow draft to review to verified to published to superseded; author and reviewer must differ",
        "Corpus versions immutable once published, with checksums verified at load",
        "Gate 11 (content) activates and fails the build on a single incomplete entry"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "content"
      ],
      "effects": [
        "E-001",
        "E-006",
        "E-013",
        "E-014"
      ],
      "plan": "plans/F-011-corpus-schema-provenance-and-content-gate.md",
      "attested": [
        {
          "criterion": "Five classifications enforced and displayed; our own curation cannot be marked historical",
          "verifiedBy": "The colour-detail surface (F-018) rendering classification on every entry, asserted in its e2e and a11y run. The ENFORCED half is gated here — parseEntry rejects any other value and the mutation proof watches a japanese-inspired entry marked historical go red — but nothing in this repository can prove a field is DISPLAYED before a surface exists.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "Editorial workflow draft to review to verified to published to superseded; author and reviewer must differ",
          "verifiedBy": "F-062 (editorial review workflow) driving an entry through the states with stored prior state, so a transition can be compared against one. The AUTHOR-AND-REVIEWER half is fully gated here and proven by three mutation cases (same id, same person under two ids, unknown id), as is every per-status obligation. The SEQUENCE half is not enforceable in a file-based corpus: nothing on disk records what status an entry held before this commit, so assertTransition has no caller and cannot have one until something stores that.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "package": "@irodora/corpus",
      "notes": "DONE. DELIVERED: the corpus schema as a parser (@irodora/corpus), the editorial workflow and identity rule, canonical JSON and the two-level checksum, the published-version bundle with verify-at-load, scripts/generate-corpus.mjs, and GATE 11 ACTIVE AND BLOCKING with a 19-case mutation proof. 211 tests. NO COLOUR WAS ADDED — content/colors/ is still empty, which is F-012. DERIVED VALUES ARE UNAUTHORABLE, not merely regenerated: parseEntry rejects lab, lch, oklch, rgb, hex and gamut BY NAME. That is one step stronger than ADR-0043, which must regenerate-and-compare because the design manifest keeps its srgb for browsers; nothing reads a hex out of a source entry, so the stronger form was available. The rejection points at color.sourceHex, because the spec section 1 example showed those keys and an editor would have copied it. THE GATE SHIPPED BEFORE ITS DATA, which is the hard part. F-011 builds the gate and F-012 supplies the entries, so on activation the corpus is EMPTY and a gate that passes over nothing is failing open for the whole of R1. Four answers: it FAILS if it cannot locate its inputs; it runs 1 valid + 18 invalid FIXTURE corpora every invocation so the rules exercised are never zero; the fixtures cannot become content (outside content/, the scan globs content/ only, and a fixture- slug under content/ is itself a failure); and verify-content-proof.mjs mutates the VALID corpus 19 ways asserting the gate goes red AND NAMES THE RIGHT FIELD, baseline green either side, with ONE case that must stay GREEN. The authored-entry count (0) prints beside the fixture count on every run. THE CI CONDITION WAS THE WHOLE BALLGAME: gate 11 step carried `if: hashFiles(content/colors) != ''`, and gate 0 compares `run:` commands without reading `if:` — so the gate would have been \"active\" and never once run in CI for the rest of R1, with nothing able to say so. Removed. The general case is F-072. THREE DOCUMENTS DISAGREED about required provenance: the spec section 1 list omitted sourceLicence, publisher, rightsHolder and editorialNotes; ADR-0007 section 1 requires them and NFR-20 names the licence. The accepted decision won and the spec was corrected. IDENTITY IS A ROSTER ID (ADR-0047) because the rule was UNENFORCEABLE — the schema had no author field at all, so \"author and reviewer must differ\" could not run. Free text would have passed \"A. Ranjan\" against \"Ashish Ranjan\", and an UNKNOWN id must FAIL rather than count as a third person, which is the half that matters. THE REGISTER CROSS-CHECK existed only as a sentence: licensing-and-provenance.md section 5 stated the content gate performs it. It did not. Now it does, and an unparseable table is a FAILURE rather than an absence of constraint. TWO DEFECTS FOUND BY TESTS DURING THE BUILD. (1) The per-entry checksum covered only the authored record, so a tampered derived hex LOADED CLEAN and apps/api would have served it (F-016); it now covers the derived block too. (2) The bundle serialised xyz as a tuple while parseEntry requires the authored {x,y,z}, so a bundle could not load its own output. AND TWO DEFECTS THE FIXTURES FOUND IN THEMSELVES: duplicate-slug was broken in two ways and failed for the wrong rule; then, filenames coming from slugs, both entries wrote to one file and it stopped being invalid at all. Both caught by the gate, not by review — which is the argument for generating fixtures rather than hand-maintaining eighteen copies. THE HUE BOUND WAS GUESSED AND WRONG. A CIELAB bound of 6 degrees for a gamut-mapped corpus hex failed at 7.97. Measured over the six Display-P3 corners: OKLCh drift ours 0.167 vs clipping 3.150; CIELAB ours 7.966 vs clipping 5.206. IN CIELAB CLIPPING LOOKS BETTER THAN WE DO — not an algorithm failure but CIELAB and OKLab disagreeing about hue exactly where the P3 primaries sit, which is why OKLab exists. gamutMap holds OKLCh hue by construction (ADR-0045), so OKLCh is the metric that describes it; the CIELAB row is asserted as a NON-claim, because picking the flattering ruler is the failure mode here. srgbToHex moved into @irodora/color-spaces and design-tokens.toHex delegates — proven safe by 96 design-tokens tests, gate 9 and all 9 contrast mutation proofs. E-001 DESTINATION HALF IS NOW GUARDED, which it has never been: the gate recomputes every derived value in the LATEST bundle under the CURRENT engine. Older versions are checksum-verified only and the gate PRINTS that it skipped them, because asserting today's engine reproduces yesterday's answer is the claim FR-10 forbids. EVALUATION FINDINGS, FIXED IN THE SAME FEATURE. (1) E-001's rationale claimed the mutation proof watched a perturbed OKLab matrix go red. IT DID NOT — there was no such case, and the evaluator ran the experiment and watched the gate stay green. The case now exists (rebuilds color-spaces and corpus, asserts gate 11 red, restores). (2) GATE 11'S ENTIRE PUBLISHED-BUNDLE BLOCK WAS UNREACHABLE: zero real bundles until F-012, no fixture carried one, and not one proof case touched a checksum — so the charter claimed checksum and E-001 destination enforcement by code that never ran. The valid fixture corpus now carries a published version and five proof cases attack it (edited entry, edited derived value, altered ledger checksum, removed ledger row, removed entry). 25 cases now, was 19. (3) E-014 recorded guard:gate:content while the real guard was gate:test — true before the fixture had a bundle, and now genuinely both. (4) The paired memory notes for E-001 and E-006 were left stale while effects.json was updated, in direct contradiction; both rewritten. (5) OUR_OWN_CURATION was exported, documented as forcing a decision, and consumed by nothing — the rule keyed on sourceType alone, which let an editorial source call itself \"traditional\" or \"modern-japanese\". Both are claims about the received canon and neither is ours to make; the rule is now a positive list and the constant is load-bearing. (6) A test asserting CLASSIFICATIONS.filter(isOurOwnCuration) equals OUR_OWN_CURATION was true by construction and could not fail; replaced with the literal complement. ONE FINDING RECORDED RATHER THAN FIXED: the workflow STATE MACHINE (assertTransition / canTransition) has no non-test call site, so criterion 4's \"draft to review to verified to published to superseded\" is not enforced as a sequence. It cannot be, in a file-based corpus: there is no previous status on disk to compare a new one against. What IS enforced is every per-status obligation — a reviewer is required from `verified` onward and forbidden before it, only published|superseded enter a bundle, and author must differ from reviewer. Sequence enforcement needs stored prior state and belongs to F-062 (editorial review workflow); the machine is built and tested here so F-062 inherits it rather than reinventing it. Recorded as an attested criterion rather than claimed."
    },
    {
      "id": "F-012",
      "title": "Japanese colour atlas seed corpus",
      "requirements": [
        "FR-20",
        "FR-21",
        "FR-22"
      ],
      "service": "content",
      "release": "R1",
      "priority": "must",
      "status": "blocked",
      "blockedBy": [
        "F-011"
      ],
      "openQuestions": [
        "OQ-5"
      ],
      "acceptance": [
        "Seed corpus published with 100 percent complete provenance and a named reviewer per entry",
        "Contemporary palette systems: Quiet Neutrals, Indigo Studies, Forest and Mineral, Earth and Clay, Seasonal",
        "Every palette has an anchor role and editorial provenance",
        "Every source appears in the register in docs/content/licensing-and-provenance.md",
        "No entry derives from a scraped or unlicensed dataset"
      ],
      "verification": [
        "state",
        "content"
      ],
      "notes": "OQ-4 REMOVED IN F-074: docs/PRD.md records it as closed — \"OQ-4 is settled at ~120 entries — depth over breadth\" — while this feature still listed it as blocking. OQ-5 remains and is the real blocker, and it is not a code problem: content/editors.json holds ONE editor, and the content gate requires two DISTINCT roster identities for a published entry (ADR-0047). No entry can be published until a second editorial identity exists, and inventing one would fabricate the provenance the roster exists to guarantee.",
      "attested": [
        {
          "criterion": "Seed corpus published with 100 percent complete provenance and a named reviewer per entry",
          "verifiedBy": "A named Japanese editorial reviewer signing off each entry (OQ-5). The content gate proves the FIELDS are complete; it cannot prove the reviewer read it.",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-013",
      "title": "Colour naming engine",
      "requirements": [
        "FR-7"
      ],
      "service": "packages",
      "package": "@irodora/color-naming",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-007",
        "F-011"
      ],
      "acceptance": [
        "Returns at least 3 ranked candidates with DeltaE00 and a similarity percentage",
        "Two-stage retrieval: coarse Lab-bucket shortlist, then exact DeltaE00 ranking in the engine",
        "A test brute-forces the full corpus and asserts the two-stage result is identical",
        "Output language is closest digital reference, never an assertion of identity; enforced by the claims lint"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "test",
        "build"
      ],
      "plan": "plans/F-013-colour-naming-engine.md",
      "attested": [
        {
          "criterion": "Output language is closest digital reference, never an assertion of identity; enforced by the claims lint",
          "verifiedBy": "F-025 (claims copy lint), which owns the banned-construction list repo-wide. F-013 delivers the STRUCTURAL half and that half is gated here: limit < 3 throws and buildNamingIndex refuses fewer than 3 records, because a single answer is an identification (ADR-0031); and no field in NamingCandidate or NamingResult can be read as an identity claim, asserted as an exact key set with a decoy carrying an exact-match flag. What F-013 cannot do is lint copy — the enforcement half is a repository-wide lint whose subject is every UI string, and building a second banned-construction list here would be the duplication defect. F-025 is NOT added to blockedBy: that inverts the dependency, since F-025 wants real copy to lint and this feature produces some of it.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "effects": [
        "E-003",
        "E-015"
      ],
      "notes": "DONE. DELIVERED: @irodora/color-naming — a Lab bucket index, a provable lower bound on ΔE00 to any box, an expanding search with a stopping rule, the ΔE00 ranking with a total comparator, the ADR-0048 similarity scale, and an adapter from a published corpus bundle. 59 tests here plus 4 in @irodora/corpus. NO CORPUS ENTRIES EXIST — F-012 is blocked on OQ-4/OQ-5 — so the suite runs on generated corpora and prints that it did. CRITERION 3 HOLDS: two-stage is byte-identical to brute force over 4,664 synthetic records x 120 adversarial queries, at four limits, at bucket steps 1/2/5/25/1e6, cross-checked against culori. MEASURED PRUNING, printed not claimed: n=45 examines 40.8%, n=416 12.9%, n=4203 2.1%. The plan warned the bound might examine ~100% at R1 sizes; it does not, and the fraction FALLS as the corpus grows. Asserted as a trend, never a threshold — a threshold at these sizes would flake and then get deleted. THE CORRECTNESS ARGUMENT IS THE FEATURE. A fixed radius — the design criterion 2 most naturally reads as — is wrong on 317 of 360 queries, first at n=45, because ΔE00 is not a metric and Lab distance does not bound it. Instead: a sound lower bound per bucket, visit in increasing bound, stop when the next cannot beat the k-th best. Correctness is then INDEPENDENT of bucket size, which the invariance test is what proves. The bound was built FIRST, out of plan order, because it was the cheapest moment to discover the algebra was wrong; four facts re-derived rather than cited, and 130k+ property samples concentrated where each could fail — plus a 200k-iteration decoy run where an unsound bound is caught and ours has zero violations. THREE THINGS TESTS FOUND THAT I HAD WRONG. (1) A property test disproved ADR-0048 on run 1: similarity is NOT strictly decreasing, because the curve is not injective in float64. The ADR now claims only monotone non-increasing — and this is an argument FOR the design, since sorting by similarity would tie where ΔE00 does not and reorder with input order. (2) The culori cross-check disagreed by 0.81 ΔE00 because culori lab is D50 and ours is D65; color-difference already calls it as lab65. A cross-check is only a check if both are asked the same question. (3) A bucket-box test assumed one bucket and got three: floor(-30/1000) is -1, so negatives land in the cell below zero. Correct behaviour, wrong expectation. A PLAN CORRECTION MADE BY typecheck IN UNDER A MINUTE: the plan specified a Color query and asserted no cycle. @irodora/color-core is the FACADE and already depends on color-naming, so depending back is a cycle by construction, and the corpus devDependency closed it again via core. The query is a Lab triple; provenance-aware wrapping belongs in the facade. The VersionBundle assignability guard moved into packages/corpus, which is the better home — the schema is that package’s contract to keep. AS A RESULT F-073 IS NOT DISCHARGED AND NOT NEEDED: the engine-to-non-engine edge does not exist rather than being contained. CRITERION 4 SHIPS HALF-GATED, declared as attested rather than discovered at the end. The structural half is enforced here — limit < 3 throws, an index of fewer than 3 records is refused, and the exact key sets of NamingCandidate and NamingResult are asserted with an exact-match-flag decoy so ADDING a field breaks the test. The lint half is F-025’s. DOCS CORRECTED: color-engine.md section 8 said \"candidate retrieval from the corpus (spatially indexed for speed)\" — defensible but the exact phrasing that invites the fixed radius, in a document the rules tell an implementer to read in full. It now states that retrieval is indexed and ranking cannot be, and carries the 317/360 measurement. E-003’s memory note said its consumers \"do not exist yet\"; that became false here and was rewritten."
    },
    {
      "id": "F-014",
      "title": "Harmony engine",
      "requirements": [
        "FR-6"
      ],
      "service": "packages",
      "package": "@irodora/color-harmony",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-009"
      ],
      "acceptance": [
        "Monochromatic, tonal, analogous, complementary, split, triadic, tetradic, neutral, near-neutral, warm/cool, value-contrast and chroma-contrast generators",
        "All generation happens in OKLCh, not HSL",
        "Editorial Japanese harmonies sourced from the corpus, kept distinct from geometric ones",
        "Every generated colour passes gamut mapping before it is returned"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "color-golden",
        "build"
      ],
      "plan": "plans/F-014-harmony-engine.md",
      "effects": [
        "E-012"
      ],
      "notes": "DONE. DELIVERED: @irodora/color-harmony — all twelve FR-6 generators in OKLCh, every output gamut-mapped and reporting what that cost, the geometric/editorial split with attribution enforced both ways, and an adapter from a published corpus bundle. 33 tests. NO PALETTES EXIST — F-012 is blocked on OQ-4/OQ-5 — so the editorial suite runs on generated bundles and prints that it did. THE STUB WAS WRONG AGAINST ITS OWN ACCEPTANCE LIST: nine kinds where FR-6 requires twelve (missing near-neutral, warm-cool, value-contrast, chroma-contrast) AND it carried `editorial` as a KIND, which conflates two axes and makes criterion 3 unexpressible — an editorial harmony is a family, not a relationship. THE PLAN WAS ALSO WRONG, and is corrected in place: it said \"an editorial harmony still stands in some relationship\". Not in general — a curator assembling Quiet Neutrals is not obliged to have picked a triad, and labelling it afterwards would invent a geometric claim they never made. So `kind` is null for editorial. THE MEASUREMENT THAT JUSTIFIES THE WHOLE DESIGN. color-engine.md asserted that HSL hue rotation is perceptually inconsistent and never measured it. Over the hue circle a 30-degree HSL rotation moves the colour by ΔE00 5.0 to 35.9 — a 7.2x SPREAD. The same rotation in OKLCh ranges 9.7 to 14.5, a 1.5x spread. Asserted on the SPREAD rather than either end, because inconsistency is the claim; and OKLab is NOT asserted to be ΔE00-uniform, which would be its own over-claim. CRITERION 4 AND FR-6 PULL AGAINST EACH OTHER, AND ADR-0045 RESOLVES IT. Mapping changes a colour, so it could break the relationship just built. It does not for HUE — gamutMap holds L and h — measured here at 6.2e-5 degrees for a complementary pair and 5.3e-5 for a triad. It DOES for chroma, so chroma-contrast has a weaker tolerance and every colour reports wasGamutMapped and gamutDeltaE00. That makes E-012 a sharper link than it was: harmony depends on the PROPERTY ADR-0045 chose, not just on the function. ADR-0049: warm and cool are a stated convention anchored to the corpus taxonomy, because every other generator is derivable and this one is not — and if the engine picked its own anchors the product would contradict `taxonomy.temperature`. The ADR is blunt about the cost: two numbers cannot express a convention that varies by culture, and this product's subject is Japanese colour. PLANNED WITHOUT THE PLANNER SUBAGENT, recorded as a deviation: AGENTS.md recommends it, and it was used for F-011 and F-013 — both plans contained a factual error about this repository (F-013's asserted \"no cycle\" when color-core is the facade). Direct authorship after building both adjacent packages was the more reliable path here. Not a licence to skip planning: the plan exists and gate 0 enforced it."
    },
    {
      "id": "F-017",
      "title": "App design system, accessibility and i18n",
      "requirements": [
        "NFR-8",
        "NFR-9",
        "NFR-11"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-003",
        "F-039"
      ],
      "acceptance": [
        "@irodora/ui ships React Native components that consume @irodora/design-tokens; no colour literal appears in any component, and the lint fails on one",
        "Every component declares its kind and defines every state that kind requires, carrying accessibilityRole, accessibilityLabel and accessibilityState rather than reimplementing them",
        "Enumerated en/ja message catalogue; a missing or an extra key fails the build, and a hard-coded user-facing string fails lint",
        "The Japanese is written and reviewed by a competent speaker, never machine-translated",
        "The Japanese line-height scale is applied and the line-breaking strategies are passed; font coverage over the corpus is checked by a proven script, and the bundled asset it needs is F-076",
        "Kinsoku line breaking is correct on a device, on iOS and on Android",
        "Zero WCAG 2.2 A/AA violations across every screen, asserted against the rendered accessibility tree rather than by inspection",
        "VoiceOver and TalkBack complete every screen, and text scaled to 200 percent loses no content or function",
        "Gate a11y activates with a CI step that actually executes, and the contrast gate gains both the colour-only status scan it records as owed to F-017 and the small-text check on largeText-only tokens, which it does not yet record at all"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "a11y",
        "contrast",
        "build"
      ],
      "effects": [
        "E-007"
      ],
      "attested": [
        {
          "criterion": "The Japanese is written and reviewed by a competent speaker, never machine-translated",
          "verifiedBy": "Review by a competent speaker recorded against a roster id (ADR-0047), the same identity mechanism the corpus uses. BLOCKED ON OQ-5, exactly as F-012 is: the engagement model for a Japanese editorial reviewer is unresolved. The catalogue MECHANISM is gated now — a missing or extra key is a typecheck failure — and the unreviewed count prints on every run so \"a missing translation fails the build\" cannot quietly become \"an unreviewed translation passes silently\".",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "Kinsoku line breaking is correct on a device, on iOS and on Android",
          "verifiedBy": "A device run on both platforms against a fixed string set — a line ending in a Japanese comma, a small kana at a line start, a long katakana loanword. React Native delegates line breaking to the platform text engine, so the JS render tree cannot see it: the gated half is that lineBreakStrategyIOS and textBreakStrategy are passed, not that they worked.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "VoiceOver and TalkBack complete every screen, and text scaled to 200 percent loses no content or function",
          "verifiedBy": "A device pass with each screen reader, per the release checklist. The gate proves the accessibility TREE, not the pixels — there is no Yoga layout in a JS render tree, so clipping, overflow and occlusion are invisible to it. The gated half is that no component disables font scaling and every text permits a multiplier of at least 2.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "The Japanese line-height scale is applied and the line-breaking strategies are passed; font coverage over the corpus is checked by a proven script, and the bundled asset it needs is F-076",
          "verifiedBy": "The gated half is done: the scale is emitted in absolute points with Japanese leading strictly greater than Latin at every step, lineBreakStrategyIOS and textBreakStrategy are passed for Japanese only, and scripts/verify-font-coverage.mjs parses cmap formats 4 and 12 with a --prove mode that builds a synthetic TTF to watch it discriminate. What remains is the ASSET: F-076 carries the Noto Sans JP subset, the subsetting pipeline, the NOTICE.md licence entry and the wiring into gate:content. Until then the app falls back to the platform face, the checker exits 1 rather than reporting a green run as coverage, and this stays outstanding.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "plan": "plans/F-017-app-design-system-accessibility-and-i18n.md",
      "notes": "DONE. 11 increments, each committed with the gates green at its own boundary. THE FEATURE OPENED BY FINDING ITS OWN CONTRACT WRONG: the acceptance criteria said to build a Next.js 16 app with Server Components, axe and a `web-perf` gate, nine months after ADR-0051 retired the web surface and four features after apps/mobile shipped. Gate 0 was green throughout, because a criterion naming a surface that no longer exists is still a well-formed string — F-074 records that guard, and the sweep also found docs/PRD.md only HALF corrected after the rehaul. FOUR ADRs: 0054 React Native primitives (0034 superseded); 0055 the a11y gate under jest-expo, DECIDED BY RUNNING IT — the spike found that jest-expo@57 is built on Jest 29 internals and dies against jest@30 before a single test, and that RNTL@14 and jest-expo@57 are not aligned; 0056 the enumerated catalogue, because fallback is the core behaviour of every runtime i18n library; 0057 a bundled Noto Sans JP subset generated from the corpus, possible only because the corpus is an immutable signed bundle at a pinned version. WHAT THE CHECKS CAUGHT, mostly in my own work: the CSS line-height ratio would have rendered a 15pt line with 1.65 POINTS of leading; icon.check/alert/cross resolved to NOTHING, so NFR-9 was true of the type and unproven of the render; the conformance suite rejected my own Swatch for missing disabled/loading and for rendering focus and active identically; gate 9 rejected the home screen for five predicted foreground.3 findings AND nine unpredicted ones, because the screen read useColorScheme() itself and so rendered LIGHT when asked for dark; a11y-scope found Status and Icon in no registry, and had a bug of its own that skipped generic components; tsc rejected textBreakStrategy: high_quality (RN wants camelCase) and found ColorSchemeName includes \"unspecified\", which is exactly the no-preference case the theme fallback exists for; and the claims lint rejected a doc comment of mine that quoted two banned phrases as examples. MUTATION RUNS THAT CHANGED THE DESIGN rather than confirming it: deleting the RN text-inheritance model turned only ONE of two tests red, because the node then falls back to 14px which is ALSO below the large-text floor — the 22px counterpart fixture exists because that experiment was run. And one a11y proof case stayed green because RN DERIVES accessibilityState.disabled from the disabled prop, so the case was wrong, not the gate. GATE 8 ACTIVATED LAST, after being watched fire six times against real components, with ciStep:true and a real CI step whose removal was watched turning gate 0 red — it had been specified as ciStep:false with the mirror check skipping it, which would have made it read `active` while never executing. SPLIT OUT RATHER THAN LEFT AS NOTES: F-074 (gate 0 sees a retired-surface criterion), F-075 (motion may not animate a colour — not a lint, because knowing the node IS a sample needs composition static analysis cannot see), F-076 (the font asset). THREE CRITERIA ATTESTED: the Japanese being reviewed rather than machine-translated (blocked on OQ-5, as F-012 is), on-device kinsoku, and VoiceOver/TalkBack with text at 200 percent. `e2e` was removed from verification because `pnpm test:e2e` exits 1 by design while no surface declares the script — verified by running it."
    },
    {
      "id": "F-018",
      "title": "Colour Atlas and colour detail",
      "requirements": [
        "FR-20",
        "FR-21",
        "FR-24"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "todo",
      "blockedBy": [
        "F-012",
        "F-017",
        "F-039"
      ],
      "acceptance": [
        "Every corpus entry reachable in 3 interactions or fewer from the atlas root",
        "Reads the signed corpus bundle at a pinned version with its digest verified at load, and renders with no network access",
        "Browsing renders values read from the published bundle; the engine is called for derived answers, never to recompute a value the bundle already carries",
        "Colour detail shows names in four forms, every coordinate system, provenance, relations, palettes and CVD appearance",
        "Every swatch has an accessible name and its numeric value"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y",
        "contrast"
      ],
      "notes": "BLOCKEDBY F-012 ADDED IN F-074. It was missing, and F-018 cannot meet its own second criterion without it: content/versions/index.json is [], so there is no signed bundle at any version to read or to verify a digest against. Found while selecting the next feature after F-017 — the same class of defect as the retired-surface criteria, and one the F-074 check CANNOT see, because it is a missing field rather than a wrong word."
    },
    {
      "id": "F-019",
      "title": "Colour Compare",
      "requirements": [
        "FR-48"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "todo",
      "blockedBy": [
        "F-018",
        "F-039"
      ],
      "acceptance": [
        "DeltaE00, per-axis differences, OKLCh delta, CVD separation and contrast, each with its units and computation space",
        "Tabular numerals, aligned columns, copyable values"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y",
        "contrast"
      ]
    },
    {
      "id": "F-020",
      "title": "Palette Studio",
      "requirements": [
        "FR-49"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-018",
        "F-039"
      ],
      "acceptance": [
        "Build, edit, reorder and save palettes with anchor, neutral, light and accent roles",
        "Palettes validate against the same schema as corpus palettes"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y",
        "contrast"
      ]
    },
    {
      "id": "F-021",
      "title": "Colour Finder",
      "requirements": [
        "FR-47"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "todo",
      "blockedBy": [
        "F-018",
        "F-039"
      ],
      "acceptance": [
        "Search by English name, Japanese name, kanji, romaji, hex, or natural phrase",
        "A hex query returns its nearest entries by DeltaE00",
        "Natural phrases map deterministically to lightness/chroma/hue regions from a versioned lexicon in content/rules"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y",
        "test"
      ]
    },
    {
      "id": "F-023",
      "title": "Shareable colour cards",
      "requirements": [
        "FR-50"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-018",
        "F-039"
      ],
      "acceptance": [
        "A rendered card with name, kanji, hex, attribution and corpus version",
        "Renders deterministically: the same entry at the same corpus version produces the same card on both platforms",
        "Readable at thumbnail size"
      ],
      "verification": [
        "state",
        "e2e",
        "contrast"
      ]
    },
    {
      "id": "F-025",
      "title": "Claims copy lint",
      "requirements": [
        "NFR-21"
      ],
      "service": "root",
      "release": "R1",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-004"
      ],
      "acceptance": [
        "Banned constructions fail lint across UI strings, marketing copy, documentation, code comments and identifiers",
        "Permissible language is bound to Provenance.source",
        "The allowlist is explicit, small, and each entry links to the measurement supporting it",
        "A deliberately added banned phrase makes the gate fail"
      ],
      "verification": [
        "state",
        "lint"
      ],
      "plan": "plans/F-025-claims-copy-lint.md",
      "notes": "DONE. DELIVERED: scripts/verify-claims.mjs inside gate 2, .harness/verification/claims.json holding the patterns as DATA, scripts/verify-claims-proof.mjs, and a clean fixture. Golden rule 11 is now a gate rather than a habit. THE PROBLEM THIS GATE HAS: the banned phrases must appear in this repository — ADR-0031 lists them, so do the governance doc, three skills and two rule files. A naive scan flags 18 files, most of them the policy. A blanket exemption for documents-discussing-the-policy would BE the gate, since every real claim in docs/ sits inside it. Answered with three exemption KINDS that are not interchangeable: policySource (an explicit file list, never a glob, 20 entries each with a reason), an inline claims-ok: marker scoped to ONE line and requiring a 12+ character reason, and `measured` requiring a link to a device-lab row. `measured` IS EMPTY and the count is printed every run — the device colour lab is F-063, and no number may exist without a row. FOUND ON ACTIVATION: the design system thesis was “soft chrome, exact colour”, a tagline asserting exactness in a product whose central commitment is that capture is an estimate. It meant the swatch carries no corner radius — rendering geometry, not accuracy — but a two-word thesis migrates into marketing faster than an identifier migrates into a field. Reworded to “unaltered colour”. Also corrected three “the true colour” comments in corpus/derive.ts to “specified”, which is more precise anyway. A FALSE POSITIVE I INTRODUCED AND FIXED BEFORE ACTIVATION: the actual-colour pattern had no determiner, so it fired on its own ADR-0052 (“covers actual colours” = real colours, ordinary English). Now requires the|its|their|your|a. PROVEN: 14 cases, each watched, baseline green either side — 12 red (one per construction plus a bare marker) and TWO green (a marker with a reason, and the clean fixture), with every red case also asserting the output NAMES the right construction. NOT DELIVERED, printed on every run: the provenance-conditional half of ADR-0031 §1. Deciding whether “measured” sits near an estimated value needs the render tree; it activates with F-017 and the table is already in claims.json."
    },
    {
      "id": "F-026",
      "title": "Guided personal colour profile setup",
      "requirements": [
        "FR-26",
        "FR-30"
      ],
      "service": "mobile",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-039"
      ],
      "acceptance": [
        "Swatch-comparison flow completes in 90 seconds median, with no camera",
        "Produces lightness range, temperature tendency, chroma tolerance, contrast preference, neutrals, accents and avoid-list, each with its own confidence",
        "The data model has no single skin-colour field, and a schema check rejects a migration adding one",
        "Every dimension is editable, and a user correction is never overwritten by re-derivation"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "e2e",
        "a11y"
      ]
    },
    {
      "id": "F-027",
      "title": "Photo-assisted profile setup",
      "requirements": [
        "FR-27",
        "NFR-23"
      ],
      "service": "mobile",
      "release": "R3",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-026"
      ],
      "acceptance": [
        "Ranges derived on-device; the image is discarded and never transmitted",
        "Every derived dimension presented as editable with its confidence",
        "The profile is never finalised without explicit user confirmation",
        "Bias validation across every ITA-degree band with a stated minimum sample per band"
      ],
      "verification": [
        "state",
        "test",
        "e2e"
      ]
    },
    {
      "id": "F-028",
      "title": "Personal compatibility engine",
      "requirements": [
        "FR-11",
        "FR-29"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-026"
      ],
      "acceptance": [
        "Score in [0,100] as a pure function of profile and colour, given the rule version",
        "Explanation objects are data: factor, contribution, direction, and an i18n message key",
        "Per-dimension confidence weights the contribution of uncertain dimensions",
        "No prose is generated at scoring time"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "build"
      ]
    },
    {
      "id": "F-029",
      "title": "Rule and weight content system",
      "requirements": [
        "FR-34",
        "FR-67"
      ],
      "service": "content",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-011"
      ],
      "acceptance": [
        "Weights and harmony rules are versioned, immutable content in content/rules",
        "Every rule carries a rationale; a weight without one cannot be published",
        "Weights sum to 1.0, validated at publish time",
        "Changing a weight changes rankings with no deployment",
        "Occasion contexts are deterministic weighting profiles"
      ],
      "verification": [
        "state",
        "test",
        "content"
      ],
      "effects": [
        "E-009"
      ]
    },
    {
      "id": "F-030",
      "title": "Outfit colour engine",
      "requirements": [
        "FR-31",
        "FR-38"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-028",
        "F-029"
      ],
      "acceptance": [
        "Given a garment colour and slot, returns 5 or more ranked trouser and 4 or more ranked shoe candidates",
        "At least three alternatives per recommendation, each labelled with the dimension it moves along",
        "Candidate generation is bounded before scoring",
        "Recommendation p95 within NFR-4"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "perf",
        "build"
      ]
    },
    {
      "id": "F-031",
      "title": "Outfit scoring and explainability",
      "requirements": [
        "FR-11",
        "FR-32"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-030"
      ],
      "acceptance": [
        "Colour harmony, personal fit, contrast, Japanese aesthetic, versatility and CVD accessibility scored separately, plus an overall",
        "All component scores are shown; the overall never replaces them",
        "Every score returns its factor decomposition with direction and magnitude"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "e2e"
      ]
    },
    {
      "id": "F-032",
      "title": "CVD outfit mode",
      "requirements": [
        "FR-35"
      ],
      "service": "mobile",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-031"
      ],
      "acceptance": [
        "Flags reduced separation and proposes an alternative with the measured improvement",
        "The improvement is reproducible from the stored envelope",
        "Reads as an observation about the outfit, not as a diagnosis of the user",
        "Permanently available in the free tier"
      ],
      "verification": [
        "state",
        "cvd",
        "e2e",
        "a11y"
      ]
    },
    {
      "id": "F-035",
      "title": "Backup, export and import — the durability story",
      "requirements": [
        "FR-58"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "acceptance": [
        "An export contains every record, tombstones included, and re-imports to an identical canonical digest of every row of every table",
        "Erasure is immediate and local: every table hard-deleted, change_log included, and the key destroyed so bytes already on disk stay unreadable",
        "A re-query against each store returns nothing; the return value of the erase call is not the proof",
        "The app offers an export before any destructive action, and erases nothing if a requested export fails",
        "The export and import journeys complete on a device, writing to and reading from a file the person chose"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "build"
      ],
      "blockedBy": [
        "F-039",
        "F-041"
      ],
      "plan": "plans/F-035-backup-export-and-import.md",
      "attested": [
        {
          "criterion": "The export and import journeys complete on a device, writing to and reading from a file the person chose",
          "verifiedBy": "A device run of the export and import flows against a real file, on iOS and Android. The MECHANISM is gated here — the round trip, every refusal, and the ordering are all tested in Node against the node:sqlite driver — but file access is behind the ArchiveSink interface precisely so the package needs no platform to be tested, which means the platform binding itself is what a device confirms. FR-58 says \"asserted in e2e\"; pnpm test:e2e exits 1 by design while no surface declares the script, and gate 7 activates with F-018/F-040.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "notes": "DONE. With no server this is the entire durability story (ADR-0051 section 5), which is why it is a first-release feature and not a follow-up. 27 tests in @irodora/store. \"BYTE-IDENTICAL\" IS INTERPRETED, IN THE OPEN. Taken literally FR-58 is unmeetable: a SQLite FILE differs in page layout, freelist state and AUTOINCREMENT sequence after an identical sequence of writes, so no import could ever satisfy it and the criterion would eventually be softened quietly instead of deliberately. The claim worth making is that the DATA round-trips, so digest() is a canonical serialisation — every table in a declared order, every row INCLUDING TOMBSTONES, every column sorted — and that is compared byte for byte, against the ORIGINAL database rather than against the archive it came from. Comparing an export to an export is an echo that passes on an exporter dropping the same column twice. THE ARCHIVE IS JSON, NOT A FILE COPY: a copy of an encrypted database is useless without the key, and shipping the key with the backup makes the encryption theatre. The stated cost is that the archive is plaintext — readable by the user, which is the point, and by anyone who obtains it, which is the price. Recorded as a decision rather than discovered later. THE LINT FOUND A DESIGN FLAW: importArchive typed its parameter as Archive, and no-unnecessary-condition flagged the format check as always-false — which is exactly the type asserting a fact about data nobody had checked. An archive arrives from a FILE the user chose. It now takes `unknown` and validates every field, and distinguishes a MISSING table (an older archive, accepted as empty) from one present but not an array (corruption, refused) — treating them alike would import garbage as emptiness. TWO REFUSALS THAT SEPARATE A RESTORE FROM A DISASTER: a NEWER schemaVersion is refused rather than guessed at, the same rule migrate() applies; and a non-empty database is refused rather than merged, because a silent merge duplicates everything a user owns and looks like it worked. ERASURE IS PROVEN BY RE-QUERY, and eraseEverything RETURNS VOID so the test FR-58 warns against cannot be written. Its decoy is the one that matters: a \"wipe\" that only tombstoned would leave every row on disk while every list came back empty — soft delete exists so a sync can tell \"deleted\" from \"never existed\", and erasure exists so nothing remains to tell anything about. Destroying the key is what makes it true of bytes already written; a file delete leaves recoverable blocks and a row-by-row delete leaves them too. THE ORDER IS THE WHOLE FEATURE. eraseWithBackupPrompt enforces offer -> write -> confirm -> erase in code, so a caller cannot get it wrong by writing the calls in the order that reads more naturally, and the test asserts the ORDER rather than the outcome — a test checking only \"a backup exists and the data is gone\" passes on an implementation that erased first and backed up an empty database. AND THE CASE THAT MATTERS MOST: if a requested export FAILS, nothing is erased. Losing the data and the backup in one action is the worst outcome available here, and it is exactly what a try/catch that carries on produces."
    },
    {
      "id": "F-037",
      "title": "Ethical guardrails and bias validation",
      "requirements": [
        "NFR-22",
        "NFR-23"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-027",
        "F-028"
      ],
      "acceptance": [
        "A schema check rejects any migration adding a skin-colour, ethnicity, age or health field",
        "No code path infers a protected characteristic",
        "Held-out validation set stratified by ITA-degree band, every band above its minimum sample",
        "Per-band accuracy published internally; a band that underperforms blocks release of the profile feature"
      ],
      "verification": [
        "state",
        "typecheck",
        "test"
      ],
      "notes": "A release blocker, not a nice-to-have. A personal-colour engine unvalidated across the full skin-tone range is not shippable.",
      "attested": [
        {
          "criterion": "Per-band accuracy published internally; a band that underperforms blocks release of the profile feature",
          "verifiedBy": "A held-out validation run over an ITA-stratified sample, published internally. A release blocker on an ethical guarantee (NFR-23).",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-038",
      "title": "Performance budgets and gates",
      "requirements": [
        "NFR-4"
      ],
      "service": "tests",
      "release": "R3",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-030"
      ],
      "acceptance": [
        "Absolute committed thresholds, never baseline deltas",
        "Backend bench boots the real runtime over a versioned deterministic corpus",
        "Frontend measured over the wire, gzipped, at the load event, under prefers-reduced-motion with CPU throttling, median of 3",
        "Each gate self-tests its own arithmetic every run",
        "Gate perf activates"
      ],
      "verification": [
        "state",
        "perf"
      ]
    },
    {
      "id": "F-039",
      "title": "App foundation: Expo shell, navigation, and offline as the only mode",
      "requirements": [
        "FR-12",
        "FR-55",
        "NFR-17"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "blockedBy": [],
      "acceptance": [
        "Expo SDK 57 / RN 0.86 on the New Architecture, building for iOS and Android through EAS",
        "The colour engine runs on device, unmodified, and reproduces the committed identity digest — the React Native leg of NFR-3, attested since F-006 and finally executable",
        "Every core journey completes with the device in airplane mode, from a cold start, on a device that has never been online",
        "No account prompt exists anywhere in the app, because there is no account",
        "The app opens no socket during any core journey, asserted by the e2e offline suite rather than by inspection",
        "EAS Build produces installable builds from a Windows workstation"
      ],
      "verification": [
        "state",
        "typecheck",
        "build"
      ],
      "attested": [
        {
          "criterion": "The colour engine runs on device, unmodified, and reproduces the committed identity digest — the React Native leg of NFR-3, attested since F-006 and finally executable",
          "verifiedBy": "Running the identity fixture through apps/mobile on a physical device under Hermes. The Node leg is gated by apps/mobile/test/engine.test.ts and by F-006; Node and Chromium both run V8, so their agreement is weaker evidence than it looks, and Hermes is the interesting engine.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "Every core journey completes with the device in airplane mode, from a cold start, on a device that has never been online",
          "verifiedBy": "The e2e offline suite on a device, once journeys exist (F-018, F-040). Gate 7 is pending and its activatesWith points here; it flips when the first journey lands.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "The app opens no socket during any core journey, asserted by the e2e offline suite rather than by inspection",
          "verifiedBy": "The same suite. The build-time half IS gated now: app.config.ts blocks android.permission.INTERNET and requests no network permission on either platform, so the app cannot open a socket even if a dependency tried.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "EAS Build produces installable builds from a Windows workstation",
          "verifiedBy": "An EAS build run against this repository from the Windows workstation.",
          "blocks": "release",
          "status": "outstanding"
        }
      ],
      "notes": "DONE. The first feature that produces something a person could hold — apps/mobile is a real Expo app: SDK 57.0.14, React Native 0.86.2, React 19.2.3, Expo Router, development client rather than Expo Go because VisionCamera is a native module and F-040 needs frame processors. Versions verified with expo install --check rather than guessed; it flagged one (react-native-screens 4.20 -> ~4.26) and now reports Dependencies are up to date. THE ENGINE IS WIRED AND EXECUTING, not merely listed: apps/mobile/src/engine.ts is the single import site, the screen computes every hex and the dE00 figure at render time, and nothing on it is a typed colour value. A dependency nobody imports passes every gate and ships nothing, which this repository has already lost six increments to. FOUND WHILE WIRING IT: @irodora/design-tokens GENERATED a React Native target and never re-exported it — nativeColors was unreachable from outside the package. Byte-compared by its own test the whole time, and importable by nobody. F-039 is its first consumer, which is why it surfaced now. A DECOY THAT MATTERS: dE00 is defined on CIELAB, and OKLCh triples are the SAME TYPE, so handing them straight to deltaE00 typechecks and returns a plausible meaningless number. The first draft of engine.ts did exactly that. The test now pins the Lab route AND asserts the wrong route differs by more than 1 dE00, so the assertion discriminates. NFR-12 AS A BUILD-TIME FACT: app.config.ts requests no network permission on either platform and BLOCKS android.permission.INTERNET, because a library that declares it would otherwise have it merged in silently. Adding it back would falsify a requirement and needs an ADR. There is no newArchEnabled flag and its absence is correct — the New Architecture has been mandatory with no opt-out since SDK 55, so the field is not part of ExpoConfig. A LINT SUPPRESSION I KEPT RATHER THAN OBEYED: no-unnecessary-condition flags the ?? guard on useColorScheme(). react-native types it as null | undefined | ColorSchemeName and tsc agrees the guard is needed — assigning null to its ReturnType compiles. The rule resolves the module differently. The guard stays, with the evidence in the comment: deleting a guard because a linter overruled a measurement is the wrong way round. THREE OF SIX CRITERIA ARE ATTESTED, and the plan says so in its own section rather than leaving a reader to work out which half is checked. The Hermes execution, the airplane-mode journey and the no-socket assertion all need a device — and the last two also need journeys, which arrive with F-018/F-040. Gates e2e and a11y stay PENDING for exactly that reason: their subject is a journey and this feature ships a floor.",
      "plan": "plans/F-039-app-foundation.md",
      "effects": [
        "E-002"
      ]
    },
    {
      "id": "F-040",
      "title": "Colour Lens: live pick, garment scan, precision pick",
      "requirements": [
        "FR-12",
        "NFR-12",
        "FR-13",
        "FR-14"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-039",
        "F-077"
      ],
      "acceptance": [
        "Capture colour space is read, never assumed; an unrecognised space is unknown and caps the confidence rather than falling back to sRGB",
        "Only a bounded numeric sample crosses the bridge; the frame is disposed on the worklet thread and never written to a file",
        "All four capture modes, each calling the engine and computing no colour of its own, enforced by a lint rather than by review",
        "VisionCamera frame processors run on a worklet thread and the UI thread never blocks on colour maths",
        "yuv pixel format with conversion in the processor; every frame explicitly disposed and the pipeline does not stall",
        "Live pick sustains at least 15 updates per second on the reference device set",
        "No socket opens during a scan"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "build"
      ],
      "notes": "SCOPE CORRECTED WHILE SELECTING IT AFTER F-035. F-040 conflated two features with different verification stories: the SAMPLING MATHS (robust statistics, rejection rules, illumination and quality classification, the confidence rules) and the CAMERA PLUMBING. The maths is fully gateable and belongs in the engine — apps/mobile/AGENTS.md: \"The engine is imported, never ported. Sampling logic, rejection rules and averaging live there\" — and E-008 records that a mobile-only re-implementation makes the same fabric measure differently on two surfaces, which NO single-platform test can see. Building the camera half first would have forced the maths into the app or stubbed it. Split to F-077, which this now depends on. ALSO CORRECTED: docs/PRD.md scheduled FR-13, FR-14 and FR-15 as \"R1 web · R3 mobile\" — a retired surface AND a release that disagreed with this feature being R2 and must. The feature list is the scope authority (AGENTS.md section 1), so the PRD columns were the stale half. F-074 could not catch this: it matches vocabulary, and a release column is neither a banned word nor a gate id. DONE, with four of seven criteria attested — a property of the feature, not a shortcoming of the work, and stated up front in the plan rather than discovered at the end. THE GATED CORE IS THE PART THAT COULD OTHERWISE ROT: only numbers cross the bridge, enforced by the TYPE (LensReading has no field a frame, buffer, path or URI could be assigned to, asserted with ts-expect-error so it fails on the unused directive if that stops being true); the colour space is read and an unrecognised value is unknown rather than sRGB; all four modes call the engine, proven by recomputing the value independently through @irodora/color-sampling and requiring an exact match. TWO LINTS DO WHAT NO TEST HERE COULD. Colour maths may not drift into the app — E-008 records that a mobile-only re-implementation makes the same fabric measure differently on two surfaces, both pass their own tests, and nothing runs both. And a frame may not reach disk: a debug write during development is how \"nothing is stored\" stops being true, and it survives review as a one-line change. Both planted and watched firing; 17 boundaries. THE OPEN QUESTION IS RECORDED AT THE SEAM rather than left to whoever first has a phone: can the engine run inside a worklet? Sampling in the worklet and aggregating on the JS thread is what ships; compiling the engine for the worklet runtime is strictly better if it works. Both share this seam. Reimplementing the arithmetic there is forbidden and lint-enforced. A FIXTURE LESSON WORTH KEEPING: three separate tests in this feature initially failed because the ILLUMINATION ceiling for `unknown` (0.6) bound before the thing under test. A region with no highlights classifies unknown, and 0.6 is low enough to mask a mode ceiling of 0.7 and equal to the unknown-space ceiling. Every one of those was the classifier being right and the fixture being wrong — but it also shows that `unknown` illumination is a strong default cap doing real work.",
      "plan": "plans/F-040-colour-lens.md",
      "attested": [
        {
          "criterion": "VisionCamera frame processors run on a worklet thread and the UI thread never blocks on colour maths",
          "verifiedBy": "A device run with the frame processor active, watching the UI thread stay responsive under load. THE OPEN QUESTION THIS SETTLES: a worklet cannot call arbitrary JavaScript, so it may not be able to reach @irodora/color-sampling. Two honest answers — sample in the worklet and aggregate on the JS thread (what src/lens/camera.ts does now), or compile the engine for the worklet runtime (strictly better, and it needs a device to find out). Both share the same seam, so choosing the second later is an optimisation rather than a rewrite. The third option — reimplementing the arithmetic in the worklet — is forbidden and there is a lint that fails the build on it.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "yuv pixel format with conversion in the processor; every frame explicitly disposed and the pipeline does not stall",
          "verifiedBy": "A device run. A retained frame stalls the pipeline within a second or two, so the symptom is obvious once a camera exists and is invisible until then. yuv is roughly 2.6x less bandwidth than rgb, which is why it is the negotiated format rather than a preference.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "Live pick sustains at least 15 updates per second on the reference device set",
          "verifiedBy": "Measured on the reference device set (FR-13). Nothing off-device can measure it, and it is the criterion most likely to force a design change once measured — it is what would push the aggregation into the worklet.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "No socket opens during a scan",
          "verifiedBy": "The e2e offline suite on a device (NFR-12). The BUILD-TIME half is already gated and has been since F-039: app.config.ts requests no network permission on either platform and explicitly blocks android.permission.INTERNET, so the app cannot open a socket even if a dependency tried. Gate 7 activates with this feature and F-018.",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-041",
      "title": "@irodora/store — SQLite, SQLCipher, migrations, sync-shaped schema",
      "requirements": [
        "FR-56",
        "NFR-17",
        "NFR-7"
      ],
      "service": "packages",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "blockedBy": [
        "F-039"
      ],
      "acceptance": [
        "SQLite behind one repository interface with a conformance suite that both drivers run — node:sqlite in CI, expo-sqlite on the device",
        "Writes succeed offline and survive a reopen; reads never block on a network",
        "The SQLCipher key lives in SecureStore, never in the app database and never in the bundle",
        "The database is actually encrypted at rest, and a write survives a force-quit mid-transaction"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "build"
      ],
      "plan": "plans/F-041-store-sqlite-sqlcipher-sync-shaped-schema.md",
      "notes": "DONE, with the device half attested rather than assumed. THE DECISION IT TURNED ON: the database cannot be tested on the thing it ships on — expo-sqlite needs a device and CI is a Windows runner with none. One repository interface, TWO drivers, ONE conformance suite run against both: node:sqlite in CI (built into Node 24, SQLite 3.53.3, zero dependencies — better-sqlite3 would have to compile on every runner) and expo-sqlite+SQLCipher on the device. The limit is carried as DATA, not as a comment: the Node driver reports encryptsAtRest:false, the suite prints which driver it ran, and a test asserts it, so a green CI run cannot be read as a statement about encryption. PRAGMA foreign_keys IS THE SUBJECT OF THE SUITE. SQLite defaults it OFF and it is PER CONNECTION, so a schema full of REFERENCES enforces nothing unless it runs every time — it looks right in the DDL and accumulates orphans in silence. The check watches a bad write FAIL rather than reading the pragma back, and writing that test found the trap underneath it: PRAGMA foreign_keys IS A NO-OP INSIDE A TRANSACTION, so the first mutation (turning it off after the user_version bump, inside the migration transaction) changed nothing and the test failed for an unrelated reason. THREE ASSERTIONS WRITTEN AGAINST TESTS THAT LOOK RIGHT AND PROVE NOTHING: durability REOPENS the database, and the driver refuses reopen() on :memory: with a message saying why, because a round-trip without a reopen tests the cache; the change log is checked for WHICH row and op in WHICH order, never for a count; and a rolled-back transaction must leave NO change-log row, because a log entry describing a write that did not happen would be applied confidently by a future reconciler. THE KEY LIFECYCLE IS BEHIND AN INTERFACE BECAUSE IT IS THE PART NO DEVICE WOULD REVEAL: a key regenerated on every launch encrypts a database nobody can open again, which presents as \"the app lost my data\" — reported once, months later, unreproducible. It is a two-line test here. Its decoy is the one that matters: a CONSTANT key would satisfy \"same on second call\" perfectly and would be a key every user shares. keyPragma refuses anything that is not 64 hex characters, because PRAGMA takes no bound parameter and the key is interpolated. A test scans shipped source for a 64-hex literal, excluding test files by path. THE GUARD NO TEST COULD REPLACE: apps/mobile bundles this package, so a node:sqlite import reachable from src/index.ts is a crash on a phone and is invisible to every gate — the tests run in Node, where it resolves perfectly. The Node driver is behind @irodora/store/node and src/drivers/node.ts is exempted by explicit path, never by glob. Guard 15, planted and watched firing. DELIBERATELY NOT DONE: Drizzle, which ADR-0051 section 2 names — it is a query builder over a driver, and adding it before the repository interface existed would have settled the interface by accident. Recorded as a follow-up rather than a silent omission. And change_log is written and read by NOTHING: ADR-0051 section 6 is explicit that it is not an outbox, because an outbox implies a destination and a delivery guarantee.",
      "attested": [
        {
          "criterion": "The database is actually encrypted at rest, and a write survives a force-quit mid-transaction",
          "verifiedBy": "Running apps/mobile/src/store/conformance.ts on a physical device, then inspecting the database file for SQLite header bytes — an unencrypted file begins with \"SQLite format 3\", an encrypted one does not. SQLCipher is not in node:sqlite, so NOTHING in CI says anything about encryption at rest; the Node driver reports encryptsAtRest:false and a test asserts it, so a green CI run cannot be mistaken for this. The force-quit case needs a real process kill mid-transaction, which is why PRAGMA synchronous is FULL rather than NORMAL. The attestation is one call rather than a day of work because the same checkStore the CI driver runs is already wired to the device driver.",
          "blocks": "release",
          "status": "outstanding"
        },
        {
          "criterion": "SQLite behind one repository interface with a conformance suite that both drivers run — node:sqlite in CI, expo-sqlite on the device",
          "verifiedBy": "The CI half is gated: node:sqlite runs all seven checks every build. The DEVICE half needs a phone, and it is the half that matters — the two drivers are not the same SQLite, and a green CI run is a claim about the device driver only as far as the shared suite reaches. runDeviceStoreConformance() returns its report rather than logging it, so the evidence can be pasted into progress.md.",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-042",
      "title": "Wardrobe model and encrypted local storage",
      "requirements": [
        "FR-39",
        "FR-41",
        "NFR-13"
      ],
      "service": "packages",
      "release": "R4",
      "priority": "must",
      "status": "backlog",
      "acceptance": [
        "Only colour and type required at creation; every other field progressively enriched",
        "Colour grouping uses perceptual distance, not hex string sorting",
        "Wardrobe images are encrypted with a device key held in the platform keystore; key rotation is exercised in a test",
        "EXIF stripped on ingest; images decoded only in the worker under hard limits",
        "No documentation or copy describes this as end-to-end encryption"
      ],
      "verification": [
        "state",
        "typecheck",
        "test",
        "e2e",
        "security"
      ],
      "blockedBy": [
        "F-041"
      ]
    },
    {
      "id": "F-043",
      "title": "Add-garment flows",
      "requirements": [
        "FR-40"
      ],
      "service": "mobile",
      "release": "R4",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-040",
        "F-042"
      ],
      "acceptance": [
        "Camera, Lens scan, manual colour and image upload",
        "Median time to add an item 20 seconds or less, measured on device",
        "Never more than two required fields"
      ],
      "verification": [
        "state",
        "e2e",
        "perf"
      ],
      "attested": [
        {
          "criterion": "Median time to add an item 20 seconds or less, measured on device",
          "verifiedBy": "Timed runs on physical devices from the reference matrix, recorded with the device and OS version",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-045",
      "title": "Outfit builder",
      "requirements": [
        "FR-33"
      ],
      "service": "mobile",
      "release": "R4",
      "priority": "must",
      "status": "backlog",
      "blockedBy": [
        "F-031",
        "F-043"
      ],
      "acceptance": [
        "Compose slots, lock items, swap colours, regenerate",
        "The same locked set and versions always regenerate the same candidates"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y"
      ]
    },
    {
      "id": "F-046",
      "title": "Preference feedback loop",
      "requirements": [
        "FR-37"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R4",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-031"
      ],
      "acceptance": [
        "Repeated selection shifts a stored, inspectable per-user weight",
        "The user can see and reset it",
        "No model training; feedback affects only the submitting user, never global ranking"
      ],
      "verification": [
        "state",
        "test"
      ]
    },
    {
      "id": "F-048",
      "title": "Wardrobe coverage and gap analysis",
      "requirements": [
        "FR-42",
        "FR-43"
      ],
      "service": "packages",
      "package": "@irodora/optimization",
      "release": "R4",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-045"
      ],
      "acceptance": [
        "Reports valid outfit count and outfits per garment; recomputes incrementally on change",
        "Gaps named in product language with the outfits they would unlock"
      ],
      "verification": [
        "state",
        "test",
        "perf"
      ]
    },
    {
      "id": "F-049",
      "title": "Duplicate detection",
      "requirements": [
        "FR-44"
      ],
      "service": "packages",
      "package": "@irodora/optimization",
      "release": "R4",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-045"
      ],
      "acceptance": [
        "Flags items within DeltaE00 5 in the same category, showing the measured difference"
      ],
      "verification": [
        "state",
        "test"
      ]
    },
    {
      "id": "F-050",
      "title": "Capsule optimiser",
      "requirements": [
        "FR-45"
      ],
      "service": "packages",
      "package": "@irodora/optimization",
      "release": "R4",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-048"
      ],
      "acceptance": [
        "Solves at least N outfits from at most M garments for a 40-item wardrobe within NFR-4",
        "Branch-and-bound with a heuristic seed and a hard time budget",
        "Returns best-so-far on expiry; the result is deterministic and reproducible"
      ],
      "verification": [
        "state",
        "test",
        "perf"
      ]
    },
    {
      "id": "F-051",
      "title": "Cost-per-wear",
      "requirements": [
        "FR-46"
      ],
      "service": "mobile",
      "release": "R5",
      "priority": "could",
      "status": "backlog",
      "blockedBy": [
        "F-045"
      ],
      "acceptance": [
        "Computed from cost and recorded wears; absent data yields unknown, never an invented estimate"
      ],
      "verification": [
        "state",
        "test"
      ]
    },
    {
      "id": "F-052",
      "title": "Shopping check",
      "requirements": [
        "FR-52"
      ],
      "service": "mobile",
      "release": "R5",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-048",
        "F-049"
      ],
      "acceptance": [
        "Outfits unlocked, personal compatibility and duplicate warning, computed against the local wardrobe offline"
      ],
      "verification": [
        "state",
        "e2e"
      ]
    },
    {
      "id": "F-053",
      "title": "Calibrated scan and reference card",
      "requirements": [
        "FR-16",
        "NFR-2"
      ],
      "service": "mobile",
      "release": "R5",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-040"
      ],
      "openQuestions": [
        "OQ-3"
      ],
      "acceptance": [
        "Detects the card, locates patches, solves a correction from observed to known values",
        "Mean DeltaE00 improves by 50 percent or more against uncalibrated on the device matrix",
        "The result is labelled calibrated; the correction matrix and its residual are stored for audit"
      ],
      "verification": [
        "state",
        "test",
        "color-golden"
      ],
      "attested": [
        {
          "criterion": "Mean DeltaE00 improves by 50 percent or more against uncalibrated on the device matrix",
          "verifiedBy": "Measurement across the physical reference device matrix, recorded in the device colour lab table (F-063)",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-054",
      "title": "Outfit scanner",
      "requirements": [
        "FR-36"
      ],
      "service": "mobile",
      "release": "R5",
      "priority": "could",
      "status": "backlog",
      "blockedBy": [
        "F-040",
        "F-031"
      ],
      "acceptance": [
        "Estimates per-garment colours with confidence and returns the full score set",
        "Classical CV only, with manual region override always available"
      ],
      "verification": [
        "state",
        "test",
        "e2e"
      ]
    },
    {
      "id": "F-055",
      "title": "Professional surfaces: Lab/LCh, dE tables, batch compare",
      "requirements": [
        "FR-28",
        "FR-61"
      ],
      "service": "mobile",
      "release": "R5",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-019"
      ],
      "acceptance": [
        "Lab and LCh shown by default, not only swatches",
        "Accepts colorimeter values, marking the resulting profile reference",
        "Reference libraries, batch compare, calibration workflow"
      ],
      "verification": [
        "state",
        "e2e",
        "a11y"
      ]
    },
    {
      "id": "F-056",
      "title": "Local exports: CSS, JSON, ASE, design tokens, PDF",
      "requirements": [
        "FR-51",
        "FR-65"
      ],
      "service": "packages",
      "release": "R5",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-055"
      ],
      "acceptance": [
        "CSV, JSON, CSS custom properties, ASE, design tokens and PDF",
        "Every export embeds the engine and corpus versions",
        "ASE round-trips through Adobe tooling",
        "A report is reproducible from its envelope"
      ],
      "verification": [
        "state",
        "test"
      ],
      "attested": [
        {
          "criterion": "ASE round-trips through Adobe tooling",
          "verifiedBy": "Opening a generated .ase in Adobe tooling and re-exporting it, recorded with the application version",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-063",
      "title": "Device colour lab",
      "requirements": [
        "NFR-2"
      ],
      "service": "tests",
      "release": "R5",
      "priority": "should",
      "status": "backlog",
      "blockedBy": [
        "F-053"
      ],
      "acceptance": [
        "Controlled measurement across the reference device matrix, all modes, all illuminants, 10 or more repeats per combination",
        "Published table of mean and p95 DeltaE00 per device, mode and lighting condition",
        "Reported confidence is correlated with measured accuracy",
        "No accuracy number is published without a row in this table"
      ],
      "verification": [
        "state",
        "test"
      ],
      "attested": [
        {
          "criterion": "Controlled measurement across the reference device matrix, all modes, all illuminants, 10 or more repeats per combination",
          "verifiedBy": "Physical measurement sessions under controlled illumination, recorded row by row in the published table",
          "blocks": "release",
          "status": "outstanding"
        }
      ]
    },
    {
      "id": "F-064",
      "title": "Pattern and multi-colour extraction",
      "requirements": [
        "FR-19"
      ],
      "service": "packages",
      "package": "@irodora/color-core",
      "release": "R5",
      "priority": "could",
      "status": "backlog",
      "blockedBy": [
        "F-040"
      ],
      "acceptance": [
        "Ranked primary, secondary and accent colours with area proportions",
        "Meets its accuracy target on the pattern test corpus for stripes, checks, colour blocks and prints"
      ],
      "verification": [
        "state",
        "test",
        "color-golden"
      ]
    },
    {
      "id": "F-065",
      "title": "Occasion and weather context",
      "requirements": [
        "FR-34"
      ],
      "service": "packages",
      "package": "@irodora/recommendation",
      "release": "R5",
      "priority": "could",
      "status": "backlog",
      "blockedBy": [
        "F-029"
      ],
      "acceptance": [
        "Occasion and weather are deterministic weighting profiles in versioned content",
        "Weather is an optional input; the recommendation is unchanged and complete without it"
      ],
      "verification": [
        "state",
        "test"
      ]
    },
    {
      "id": "F-067",
      "title": "Cross-theme salience hierarchy for the status tokens, and the APCA floor for dark error",
      "requirements": [
        "NFR-8",
        "NFR-9"
      ],
      "service": "content",
      "release": "R1",
      "priority": "should",
      "status": "done",
      "blockedBy": [
        "F-003"
      ],
      "acceptance": [
        "The salience rank of status.ok, status.warn and status.bad against each theme own ground is the same in both themes",
        "Every status token meets the APCA Lc 45 large-text floor against every surface it is declared on, or its restriction is recorded as a machine-checkable field",
        "The chosen rank is recorded with its reason, not inferred from the values",
        "The contrast gate asserts the rank, so it cannot drift back"
      ],
      "verification": [
        "state",
        "contrast",
        "cvd"
      ],
      "notes": "FOUND BY THE F-003 DESIGN AND COLOUR-SCIENCE REVIEWS. PRE-EXISTING — not introduced by F-003. TWO DRIVERS, ONE DECISION. (1) SALIENCE INVERSION: measured against each theme own background, the dark theme says caution is the loudest state and error the quietest, while the light theme says error is loudest by nearly 2x. A user toggling the theme gets an inverted status hierarchy. The cause is that the approved system holds the rank of OKLCh L constant across two grounds of OPPOSITE polarity, and L rank does not survive that flip — the invariant that makes the themes one system is rank of CONTRAST against own ground. (2) THE APCA FLOOR: dark.status.bad sits at APCA Lc -37.5 to -38.6 against its three declared surfaces — below the Lc 45 LARGE-text floor of 0.98G-4g — while WCAG reads 4.92 to 5.58 and passes. ADR-0044 classifies status.* as usage:text precisely because the product tints the label, so this is body copy below even the large-text floor in the DEFAULT theme. MEASURED AND INFEASIBLE SEPARATELY: holding status.ok and status.warn at their current values, NO value of status.bad reaches even Lc 40 while keeping CVD separation at or above 60 — the APCA floor and the separation minimum are in direct tension at the current lightness arrangement. A jointly feasible solution exists and was computed during F-003 — dark ok L0.67 C0.12 H158, warn L0.70 C0.14 H70, bad L0.82 C0.10 H18, worst separation 63.1, every token above Lc 45 — but it makes error the LIGHTEST token in the dark theme, which is exactly the salience re-arrangement in driver (1). That is a redesign larger than either defect and belongs to a person rather than to a gate-building task. Until it is decided, gate 9 prints the three pairings in a separate RED band on every run so they cannot be mistaken for the ordinary WCAG/APCA disagreements.",
      "plan": "plans/F-067-cross-theme-salience-hierarchy.md",
      "effects": [
        "E-007"
      ]
    },
    {
      "id": "F-068",
      "title": "Swatch edge treatment against an arbitrary sample",
      "requirements": [
        "NFR-8"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-003",
        "F-017"
      ],
      "acceptance": [
        "A sample of ANY colour has a perceptible edge against the swatch.well beneath it",
        "The treatment is verified against the worst-case sample, not a convenient one",
        "swatch.hairline role text describes exactly what is verified"
      ],
      "verification": [
        "state",
        "contrast",
        "a11y"
      ],
      "notes": "FOUND BY THE F-003 DESIGN REVIEW. swatch.hairline claimed its edge was perceptible against ANY value; a single-tone 16 percent white inset cannot deliver that against a light sample, by construction, and no fixed-token check can cover an arbitrary garment colour. The claim was softened in F-003 (golden rule 11 applies to the design system own claims) and the treatment that WOULD deliver it — a two-tone inset, or an inset ring plus an outer rule against the well — needs components. ALSO HERE: the review asks that a real dark garment colour be put on the corrected swatch.well (#2B2A28 dark, which is darker than the #383533 the design review saw, because the hex was the defect ADR-0043 fixed) and looked at. A navy or charcoal sample on that well is near 1:1 in luminance and the hairline becomes the only edge cue."
    },
    {
      "id": "F-069",
      "title": "Status colour may not sit beside a colour sample",
      "requirements": [
        "NFR-8",
        "FR-18"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-017"
      ],
      "acceptance": [
        "A status token adjacent to a colour sample without the swatch.well separator is a lint or gate failure",
        "The rule is stated in DESIGN-SYSTEM.md beside the existing swatch.well rule"
      ],
      "verification": [
        "state",
        "contrast",
        "a11y"
      ],
      "notes": "FOUND BY THE F-003 DESIGN REVIEW. Nothing in the manifest stops a saturated status colour sitting next to a sample, and simultaneous contrast means it pushes that sample the opposite way — a deep red beside a swatch makes the swatch read green. The Lens result is exactly where a low-confidence bad indicator wants to live, beside the colour. The swatch.well separator is already mandatory for decorative colour; this extends the same rule to the semantic tokens and makes it checkable."
    },
    {
      "id": "F-070",
      "title": "border.strong meets 3:1 as an outlined control boundary",
      "requirements": [
        "NFR-8"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-003"
      ],
      "acceptance": [
        "border.strong reaches 3:1 against every surface it can sit on, under BOTH the linear and the encoded compositing model",
        "It is declared in pairsWith rather than carrying an uncheckedReason, so the gate checks it",
        "The design rule that a border is never a solid grey is either met or deliberately amended"
      ],
      "verification": [
        "state",
        "contrast"
      ],
      "notes": "FOUND BY THE F-003 COLOUR-SCIENCE REVIEW. border.strong role is Outlined controls, which is a UI component boundary under WCAG 1.4.11 and therefore owes 3:1 — it is not the decorative-hairline exemption that covers border. MEASURED over its own declared grounds: dark 3.11:1 linear (worst ground surface.3) but only 1.41:1 encoded (over background); light 1.17:1 linear and 1.41:1 encoded (over surface.3). It fails under the encoded model in both themes, and the encoded model is what a browser and React Native actually draw. THE CONFLICT: reaching 3:1 needs roughly 53 percent alpha, which is a solid grey line, and the design system states a border is a translucent overlay and NEVER a solid grey because depth must read as lift rather than outline. So this is a design decision, not a value tweak — either the outline stops being the only thing identifying the control (a filled or tinted control state would remove the 1.4.11 obligation), or the never-a-solid-grey rule is amended. F-003 made the token VISIBLE rather than fixing it: it carries an uncheckedReason with these ratios in it, and the gate now fails any token that is covered by nothing and does not declare why."
    },
    {
      "id": "F-071",
      "title": "The property-test gates are deterministic",
      "requirements": [
        "NFR-19"
      ],
      "service": "tests",
      "release": "R1",
      "priority": "should",
      "status": "done",
      "acceptance": [
        "Every fast-check property runs from a recorded seed, so a failure is reproducible and a pass is not luck",
        "No gate can go red for a reason unrelated to the change under test",
        "A test whose tolerance is within a small factor of the true error bound states the measured margin"
      ],
      "verification": [
        "state",
        "test",
        "color-golden"
      ],
      "notes": "DONE. DELIVERED: all three acceptance criteria, and the third one turned out to be a real defect rather than a tidy-up. (1) SEEDS — 41 of 48 fc.assert calls ran unseeded across 8 files; every property in the repository now carries a distinct recorded seed, 56 in total, uniqueness asserted by a grep rather than by assumption. (2) TIMEOUTS — 30 CPU-bound properties inherited vitest's default 5000 ms because packages ran bare `vitest run` with no config at all. Twelve config files now set 120 s. Proven by FIVE consecutive full `turbo run test --force` runs, 25/25 tasks each, against three separately observed flakes beforehand (testing/PRNG at 7061, 6863 and 5299 ms; color-naming/bound at 5052 ms). (3) THE TOLERANCE WAS NOT A MARGIN PROBLEM, IT WAS WRONG — F-071 recorded oklab round-trip as failing once at 1.2477e-12 against 1e-12, \"a 25 percent overshoot\". Measured over 2,000,000 cases from that generator the worst error is 5.422e-8: a factor of 54,000, not 25 percent. It passed because 5,000 unseeded samples almost never reach the tail, and seeding it WITHOUT measuring first would have frozen that luck in permanently. The cause is conditioning, not a defect: oklabToXyz cubes LMS-prime and xyzToOklab cube-roots it back, and d/dx x^(1/3) diverges at zero — the worst case has LMS-prime = [0.203, -3.7e-5, -0.488], whose cubes span a ratio of 2.3e12. Settled by ADR-0052: the claim splits into a measured 1e-6 over the full declared range (18x margin) and 1e-12 where LMS is well conditioned (21x margin), each stating its measured worst case and sample size. Real colours are unaffected — the stratified sRGB test holds at 1e-14 and in-gamut probes measure 2.78e-16. DISCRIMINATION PROVEN, not assumed: a 1e-5 perturbation of XYZ_TO_LMS_OKLAB[0][0] turns BOTH new seeded properties red, so the seeds catch a real engine change rather than having narrowed coverage to nothing. A DEFECT I INTRODUCED AND CAUGHT: the batch script that rewrote the configs reported \"updated 5 existing\" having updated 2 — three files used a single-line body the regex missed, and were left importing a helper they never called. Found by the contrast mutation proof, whose BASELINE went to exit 2. Recorded as memory/lessons/a-batch-edit-that-reports-its-own-success-is-not-evidence.md. ALSO REJECTED AND RECORDED: a root vitest.shared.ts, which every package tsconfig refuses with TS6059 because rootDir is the package and *.config.ts is deliberately included; and moving it into @irodora/testing, which would make every package's config fail to LOAD whenever that package's build broke. The timeout is therefore repeated in 12 files with the reasoning in each. NOT DONE: nothing. All three criteria are met and each was watched to fail.",
      "plan": "plans/F-071-property-test-gates-are-deterministic.md",
      "effects": [
        "E-001"
      ]
    },
    {
      "id": "F-072",
      "title": "Gate 0 sees a CI step that is conditioned out",
      "requirements": [
        "NFR-24"
      ],
      "service": "tests",
      "release": "R1",
      "priority": "should",
      "status": "done",
      "acceptance": [
        "An active gate whose CI step carries an `if:` fails gate 0, naming the gate",
        "verify-gate-mirror.mjs proves that check by adding a condition to a real step and watching gate 0 go red",
        "Every remaining conditional step for an active gate is either unconditional or justified in gates.json"
      ],
      "verification": [
        "state"
      ],
      "notes": "DONE. Gate 0 compared whole run-commands and NEVER READ `if:`, so a gate could read active, have a step, pass the mirror check, and never once execute. FOUND SEVEN OF ELEVEN ACTIVE GATES CONDITIONED OUT: typecheck, lint, format, test and build on hashFiles(pnpm-lock.yaml); color-golden on a package.json; and — the worst — cvd on hashFiles(packages/cvd-engine/dist), A BUILD OUTPUT, so a build that produced nothing would have silently skipped the gate rather than failing. None was skipping yet; all seven were one rename away, with nothing to report it. All seven now unconditional: `pnpm install --frozen-lockfile` already fails the job when the lockfile is missing, so the hashFiles guards protected nothing that was not already protected. Also removed the same condition from the Set up pnpm, Install and dependency-audit steps. THE ESCAPE HATCH: a conditional step is not always wrong — a push-only benchmark is a cadence choice — so gates.json may declare ciCondition {condition, why}, with why required at 20+ characters for the same reason F-025 requires a reason on claims-ok: an exemption nobody had to justify is a way to turn a blocking gate off. PROVEN IN ALL THREE DIRECTIONS, by hand and recorded: declared + real reason stays GREEN (the hatch works, which matters because an untested escape hatch is a wall for whoever first needs it); declared + trivial reason goes RED; declared condition not matching the workflow goes RED, because a stale declaration reads as reviewed while describing something no longer there. verify-gate-mirror.mjs now runs 22 cases rather than 11: it REMOVES each active gate step and, separately, CONDITIONS IT OUT with `if: false` while leaving the run: in place — the quiet failure the loud one hides. SCOPE, stated rather than left to be wondered about: the check applies to ACTIVE gates only. Gate 12 (perf) is pending and keeps its push-only condition; blocking a pending gate from being staged conditionally would make the check hostile to its own purpose.",
      "plan": "plans/F-072-gate-0-sees-a-conditioned-out-ci-step.md"
    },
    {
      "id": "F-073",
      "title": "Engine purity follows @irodora/* dependency edges",
      "requirements": [
        "NFR-3",
        "NFR-24"
      ],
      "service": "tests",
      "release": "R1",
      "priority": "should",
      "status": "done",
      "acceptance": [
        "A package outside the engine zone that an engine package depends on is held to the same platform-API and runtime-dependency rules, transitively",
        "scripts/verify-engine-purity.mjs --prove plants exactly that violation and watches the check fail"
      ],
      "verification": [
        "state",
        "lint"
      ],
      "notes": "DONE. The engine zone is now a GRAPH rather than a naming convention. verify-engine-purity.mjs scoped itself by package NAME (color-* and cvd-engine) and treated every @irodora/* specifier inside those as allowed WITHOUT FOLLOWING THE EDGE — so an engine package could depend on a workspace package that imports node:fs, or that declares a third-party runtime dependency, and every gate stayed green while NFR-3 was broken. A transitive node:fs breaks byte-identical output in Node, the browser and React Native exactly as directly as a local one. The zone is now the transitive closure of @irodora/* dependency edges from the declared roots, and every member is held to both existing rules. NOTHING WAS VIOLATING IT: today the closure equals the roots exactly, and the check SAYS SO on every run rather than printing a bare count — a count cannot distinguish \"no transitive packages\" from \"did not look\". That is a fact about this commit, not a property anyone was maintaining: F-011 already hit the hazard once, expecting color-naming to import @irodora/corpus, and handled it by hand with a portability override plus boundary guard #11. One package, not a rule. PROVEN: --prove now runs three planted violations rather than two. The new one is the case the old check COULD NOT SEE — color-spaces gains a dependency on @irodora/recommendation, which is not a root, and that package src gains a node:fs import. Caught, with the clean baseline asserted first and last, and every fixture verified removed. A failure message now NAMES THE EDGE that pulled a package in, so it reads as \"corpus is in the zone via @irodora/color-naming\" rather than leaving someone to ask why corpus is an engine package. THE COST, stated rather than discovered: a package pulled into the zone inherits the whole constraint set, so wanting an engine package to use a helper that reads a file is now a design decision rather than an accident.",
      "plan": "plans/F-073-engine-purity-follows-dependency-edges.md"
    },
    {
      "id": "F-074",
      "title": "Gate 0 catches an acceptance criterion that names a retired surface",
      "requirements": [
        "NFR-24"
      ],
      "service": "tests",
      "release": "R2",
      "priority": "should",
      "status": "done",
      "blockedBy": [],
      "acceptance": [
        "An acceptance criterion or a PRD verification column naming a retired surface — an HTTP route, a web-only store, a per-tenant key, or a gate id absent from gates.json — fails gate 0, naming the feature and the phrase (retired-ok: this criterion enumerates what the check must catch)",
        "The check proves itself by planting exactly that phrase into a real feature and watching gate 0 go red, then restoring it",
        "The survivors outside R2 are corrected: F-002's OpenAPI leg, which packages/contracts already records as removed; F-038's retired web performance gate, which is not in gates.json; and F-042's per-tenant data keys, which ADR-0051 retired along with multi-tenancy (retired-ok: this criterion names the defects it exists to correct)"
      ],
      "verification": [
        "state"
      ],
      "notes": "FOUND WHILE CLAIMING F-017, whose own acceptance still named Next.js 16, Server Components, axe and a `web-perf` gate nine months after ADR-0051 retired the web surface and four features after apps/mobile shipped. The R2 criteria were rewritten in F-017 and docs/PRD.md FR-20 and FR-50 were corrected with them. THIS FEATURE IS THE PART THAT MATTERS: nothing failed. Gate 0 checks that every requirement id resolves, that every path exists and that the ADR index is consistent — and passed green the whole time, because a criterion naming a surface that no longer exists is still a well-formed string. A feature list is the scope subsystem; a stale criterion in it is not cosmetic, it is a contract that would have had F-017 build a Next.js app. The guard belongs on gate 0 for the same reason the golden-rule scan does: prose in a state file rots silently and nothing else reads it. SCOPE CONFIRMED BY THE F-017 EVALUATION: the first sweep claimed exactly three survivors and missed two more — F-041 still said \"Tokens and keys in SecureStore\" on the line above one the same commit rewrote, and docs/PRD.md still measured accessibility as \"axe A/AA violations in the gate\", a live metric bound to the gate F-017 owns. Both are fixed now, and both are exactly what this guard must catch: a human sweeping by eye missed two of five. The check must cover the PRD metrics table and every acceptance array, not only the criteria a reader happens to be looking at. DONE. THE CHECK IS TWO MECHANISMS AND THE DIFFERENCE IS THE POINT: gate ids are DERIVED from gates.json, so retiring a gate needs no edit anywhere and every criterion still naming it fails on the next run; surface vocabulary is DECLARED, because \"tenant\" is not a symbol anywhere, and each term cites the record that retired it so the citation prints on failure. IT CAUGHT ALL THREE KNOWN SURVIVORS IMMEDIATELY — F-002 OpenAPI, F-038 web-perf, F-042 per-tenant — plus two deliberate mentions in F-074 itself. A GATE-ID BUG FOUND BY RUNNING IT: the first pattern anchored on the word \"Gate\" and so read \"Gates 12 (perf) and 13 (web-perf)\" as naming only `perf`, walking straight past the one that was actually retired. Now the string is first qualified as being about gates, then every `N (id)` in it is checked. THE DECOY THAT KEEPS IT USABLE: a criterion may name a retired surface in order to FORBID it — F-074's own two criteria do, and so does ADR-0051 — so `retired-ok: <reason>` is the escape hatch, the same visible-and-reasoned shape as claims-ok and ciCondition. Proven by planting an HTTP route, a per-tenant IndexedDB cache and a dead gate id and watching each reported by feature and phrase, then planting a criterion that FORBIDS a retired store and watching it stay silent. WHAT IT CANNOT SEE, printed on every run: it matches vocabulary. F-018 lacking blockedBy F-012 while being unable to read a corpus bundle is the same CLASS of defect and no word-matcher finds it — that one was fixed by hand and recorded on F-018. ALSO CORRECTED: F-012 listed OQ-4 as blocking while docs/PRD.md records it closed (\"settled at ~120 entries\"). OQ-5 remains, and it is not a code problem — content/editors.json holds ONE editor and the content gate requires two distinct roster identities, so no corpus entry can be published until a second editorial identity exists.",
      "plan": "plans/F-074-gate-0-catches-a-retired-surface-criterion.md"
    },
    {
      "id": "F-075",
      "title": "Motion may not animate a colour, and a gate can see it",
      "requirements": [
        "NFR-8",
        "NFR-9"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "should",
      "status": "todo",
      "blockedBy": [
        "F-017"
      ],
      "acceptance": [
        "An animated backgroundColor, borderColor or tintColor on a colour sample fails a check, naming the component and the property",
        "The check proves itself by planting exactly that animation and watching it go red, then restoring",
        "motion.forbidden in the manifest is the source of the banned list, never a copy of it in the checker"
      ],
      "verification": [
        "state",
        "lint",
        "test"
      ],
      "notes": "SPLIT OUT OF F-017 INCREMENT 8 RATHER THAN LEFT AS A NOTE. The colour-literal lint landed; this did not, and the reason is that it is not a lint. `motion.forbidden` says 'background-color on a swatch' and 'cross-fade between samples' — both require knowing that the animated node IS a sample, which static analysis cannot see: the component is `Animated.View`, the property is a variable, and whether it is a swatch depends on composition. A rule matching every animated colour anywhere would fire on a legitimate opacity cross-fade of chrome and be switched off within a week, which is worse than no rule. THE REASON IT MATTERS: intermediate frames of a colour transition are PLAUSIBLE colours that never existed, so a user reads a value the engine never produced — and for a colour product that is a correctness defect, not a polish one. The likely shape is a rendered check in the conformance suite rather than a lint: the tree walkers already resolve what a node paints, and an Animated node carries its driver in props. `nativeMotion.forbidden` is already emitted to the app for exactly this, so the checker reads the manifest rather than carrying its own copy."
    },
    {
      "id": "F-076",
      "title": "The Noto Sans JP subset asset, and the coverage check becomes blocking",
      "requirements": [
        "NFR-11"
      ],
      "service": "mobile",
      "release": "R2",
      "priority": "must",
      "status": "todo",
      "blockedBy": [
        "F-017"
      ],
      "acceptance": [
        "A Noto Sans JP subset ships in apps/mobile/assets/fonts, recorded in NOTICE.md with its version, source and SIL Open Font License 1.1",
        "The subset is GENERATED from the union of the published corpus, the ja catalogue, kana and Japanese punctuation — never hand-picked, and never a standard character set standing in for our content",
        "typography.families gains a React Native form and emitReactNative emits it, replacing the deliberate omission F-017 left",
        "verify-font-coverage.mjs runs inside gate:content and is blocking, so a corpus publish that introduces an uncovered character fails the build naming the entry and the codepoint"
      ],
      "verification": [
        "state",
        "content",
        "test"
      ],
      "notes": "SPLIT OUT OF F-017 INCREMENT 9. THE CHECK IS BUILT AND PROVEN; ONLY THE ASSET IS MISSING. scripts/verify-font-coverage.mjs parses cmap formats 4 and 12, collects required codepoints from the corpus and the ja catalogue, and `--prove` builds a synthetic TTF in memory to watch it report a present glyph as covered and an absent one as missing — so it discriminates today rather than being trusted until the day it matters. It currently EXITS 1 with 'no bundled font asset', which is deliberate: the app falls back to the platform face right now, so 'every kanji renders' is unverified rather than verified, and a green exit would say the opposite. It is therefore NOT yet wired into gate:content — wiring a check that cannot pass would make gate 11 permanently red. WHY IT IS A SEPARATE FEATURE: acquiring and committing a multi-megabyte font binary, plus the subsetting pipeline that regenerates it, is not something the F-017 session could do, and faking it would have been worse than recording it. THE DECOY IS 纁 U+7E81 (sohi), a real traditional colour name outside JIS X 0208 Level 1+2 — verified rather than assumed, because the first draft used U+7E41, which is 繁, a common kanji, and a decoy that is not the thing it claims to be is one nobody will re-examine."
    },
    {
      "id": "F-077",
      "title": "Colour sampling and capture assessment in the engine",
      "requirements": [
        "FR-15",
        "FR-17",
        "FR-18"
      ],
      "service": "packages",
      "release": "R2",
      "priority": "must",
      "status": "done",
      "blockedBy": [],
      "acceptance": [
        "Robust statistics over a sampling region: median, trimmed mean, variance and a confidence, over at least 1000 samples",
        "Rejection rules for specular highlights, extreme shadows, transparent and background pixels, each with a test that a rejected pixel would have changed the answer",
        "Illumination is classified as daylight, warm indoor, cool indoor, mixed, low light or unknown, and mixed or low light reduces the reported confidence",
        "Capture quality is classified excellent, good, fair or poor from exposure, blur, illumination uniformity, sample area and colour variance; poor blocks a confident claim and returns an actionable instruction",
        "Averaging happens in linear light, and a golden case proves the non-linear route is measurably darker",
        "Zero runtime dependencies, no node: imports, no platform APIs — the engine purity rules apply in full"
      ],
      "verification": [
        "state",
        "typecheck",
        "lint",
        "format",
        "test",
        "color-golden",
        "build"
      ],
      "notes": "SPLIT OUT OF F-040 WHILE SELECTING IT. F-040 mixed the sampling MATHS with the camera PLUMBING, and the two have opposite verification stories: this half is fully gateable with no device, and the other half is almost entirely a device attestation. Keeping them together would have meant either stubbing the maths or writing it in apps/mobile — which apps/mobile/AGENTS.md forbids in terms (\"the engine is imported, never ported\") and which E-008 records as the defect no single-platform test can see, because the same fabric would measure differently on two surfaces. THE TRAP THIS FEATURE MUST NOT FALL INTO is recorded in memory already: averaging non-linear sRGB is the most common colour bug there is and it always makes the result too dark [[averaging-non-linear-srgb-reads-too-dark]]. That is why a golden case proving the wrong route is measurably darker is an acceptance criterion rather than a test someone might write. DONE. TWO DESIGN FLAWS THE TESTS FOUND, both in code I had just written and both invisible to a reader. FIRST: the shadow threshold rejected the garment. The draft cut at 0.02 LINEAR — about sRGB 0.14 — and an ordinary navy, rgb(0.10, 0.10, 0.12), was discarded as shadow. This corpus is full of very dark traditional colours, so a cut placed at \"looks dark\" rejects the subject rather than the shadow. It is now 0.0015 linear, the SENSOR NOISE FLOOR, where hue actually stops being recoverable; relative darkness is the background rule's job and that one is median-relative. SECOND, AND WORSE: the illumination classifier was STRUCTURALLY BLIND TO WARM LIGHT. It read the illuminant from the pixels the material rule rejected as specular — which sounds elegant and is wrong, because that rule cuts at 0.9 linear luminance and a warm highlight is red-weighted. Green carries 0.7152 of luminance, so a tungsten highlight tops out near 0.87 and is never rejected. The classifier therefore only ever saw highlights that were already near-white, and could not classify the one condition it most needed to. It now takes the brightest quantile of the region itself, with a separate test that those pixels are genuinely brighter than the region median — because pale fabric has a brightest 5% too, and reading the illuminant off it describes the MATERIAL as the light, which is the exact failure the module header says it refuses to commit. Cut points recalibrated to ratios a real highlight produces, computed rather than guessed. THE GOLDEN CASE ASSERTS THE ERROR AS A NUMBER: 0.2354 of full scale, 60 levels on an 8-bit channel, between the linear-light mean and the encoded one. averageEncoded() is exported because it is wrong — it is the implementation almost everyone writes first. And my hand-computed expected value was wrong while the code was right (1.055 inside the power instead of outside), which is exactly what an independent recomputation is for. EVERY REJECTION RULE IS PROVEN BY THE PIXEL IT REJECTS — each case asserts the rejected pixel WOULD HAVE CHANGED THE ANSWER — with a clean-region baseline beside them, without which every case would pass on a rule that rejects everything. CONFIDENCE IS CAPPED, NEVER ASSERTED (ADR-0031, NFR-21): mixed and low light do not make a reading wrong, they make it less trustworthy, and the two ceilings combine as a MINIMUM rather than a product — a capture that is excellent under mixed light is still under mixed light. Quality blocks a claim rather than decorating one, and names the FIRST thing to fix, because an instruction naming the second-most-important problem gets followed and does not help. BLUR AND UNIFORMITY TAKE A REGION WITH DIMENSIONS, not a flat list: they are spatial properties, and a function taking `blur` as a parameter someone else computed is the self-declared-metric shape — a caller that computed it wrongly would be invisible to every check. Uniformity is measured across QUADRANTS rather than per-pixel, because per-pixel variance cannot tell a shadow across the frame from a patterned fabric and only the first is a lighting problem. ALL THRESHOLDS ARE DECLARED DATA WITH A STATED BASIS. NFR-2: no number without a row. F-063 produces the rows and is R5, so until then these are stated conventions and the confidence they produce is capped rather than asserted.",
      "plan": "plans/F-077-colour-sampling-and-capture-assessment.md"
    }
  ]
}
