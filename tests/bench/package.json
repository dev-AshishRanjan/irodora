{
  "name": "@irodora/bench",
  "version": "0.0.0",
  "private": true,
  "description": "Performance benchmarks against absolute committed budgets (NFR-4). Gate 12.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo",
    "bench": "node src/bench.mjs"
  },
  "dependencies": {
    "@irodora/color-core": "workspace:*",
    "@irodora/recommendation": "workspace:*"
  }
}
