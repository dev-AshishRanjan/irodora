{
  "name": "@irodora/e2e-full",
  "version": "0.0.0",
  "private": true,
  "description": "Full-stack end-to-end against one live deployment. From F-044.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
